

==================================================
저장한 시간 - 2026-03-24 17:16:15.0866368
==================================================

//----------------------------------------------
// 2026 1학기 STL 월56 화 78      2026.03.10(화)
//----------------------------------------------
// 스마트 포인터 -> 콜러블 타입 -> 실습 -> STL
//----------------------------------------------
#include <iostream>
#include <numeric>
#include "save.h"
using namespace std;

// [문제] 사용자가 입력한 수만큼 int를 저장할 메모리를 확보하라.
// 1부터 시작하는 정수로 메모리를 채워라
// 합계를 화면에 출력하시오

//--------
int main()
//--------
{

    save("메인.cpp");

    // int* p; -> 사용금지
    // C++11(Modern C++)에 완벽한 대체수단이 있다 -> smart_point

    unique_ptr<int[]> p;

    while (true) {
        size_t num;
        cout << "입력 - ";
        cin >> num;
        
        try {
            p.reset( new int[num] );
        }
        catch (std::exception& e) {
            cout << "메모리가 고갈 - " << e.what() << endl;
        }
        // 메모리 관리자에서 num만큼의 크기를 요청함.
        // 메모리를 다 쓰면 -> 페이징함.
        // 최근에 가장 안 쓴 메모리를 하드디스크에 저장하고 메모리를 줌.

        iota(p.get(), p.get() + num, 1);    // p위치부터 p+num위치까지 1부터 채우기

        //long long sum{};
        //for (int i{}; i < num; ++i) {
        //    p[i] = i + 1;
        //    sum += p[i];
        //}

        long long sum = accumulate(p.get(), p.get() + num, 0LL); // static_cast<long long>(0)
        // 시작위치, 마지막 위치, 시작할 값

        cout << sum << endl;

        // delete[] p; -> 이거 안 해도 됨
    }

    // free - C
    
    
}


// 이 코드는 100점!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!

==================================================
저장한 시간 - 2026-04-06 13:45:49.6999115
==================================================

//----------------------------------------------
// 2026 1학기 STL 월56 화 78      2026.03.10(화)
//----------------------------------------------
// C++에서 template의 역할은 무엇인가? - Generic Programming 구현하는 핵심키워드
// C++언어의 paradigm
// 
// 다음 주 - 많은 수의 자료를 다루기
//----------------------------------------------
#include <iostream>
#include <array>
#include <algorithm>
#include "save.h"
using namespace std;

bool 정렬기준(int a, int b)
{
    cout << "함수 ";      // 10 * (3~4) = 30~40회 호출됨
    return a < b; // 오름차순
    // return a > b; // 내림차순
}

class Dog {
public:
    bool operator()(int a, int b) {
        cout << "도그 ";
        return a < b;
    }
};

//--------
int main()
//--------
{
    array<int, 10> a{ 8,4,2,0,1,9,7,5,6,3 };

    /*sort(a.begin(), a.end(), [](int a, int b) {
       cout << "람다 ";
       return a < b;
       });*/

    sort(a.begin(), a.end(), Dog{});

    for (int num : a)
        cout << num << ' ';

    save("메인.cpp");
}

==================================================
저장한 시간 - 2026-04-06 14:13:52.4356636
==================================================

//----------------------------------------------
// 2026 1학기 STL 월56 화 78      2026.03.10(화)
//----------------------------------------------
// C++에서 template의 역할은 무엇인가? - Generic Programming 구현하는 핵심키워드
// C++언어의 paradigm
// 
// 다음 주 - 많은 수의 자료를 다루기
//----------------------------------------------
#include <iostream>
#include <random>
#include <string>
#include <print>
#include "save.h"
using namespace std;

default_random_engine dre;
uniform_int_distribution uid(0, 999'999);
uniform_int_distribution uidNameLen(1, 150);
uniform_int_distribution<> uidChar('!', '~');   // printable characters


class Dog {
public:
    Dog() {
		id = uid(dre);

		int Len = uidNameLen(dre);
        for (int i = 0; i < Len; ++i)
            name += uidChar(dre);
    }


private:
    string name;    // [1, 150]
    int id;         // [0, 999'999]

    friend ostream& operator<<(ostream& os, const Dog& dog) {
        print(os, "[{:7}] - {}", dog.id, dog.name);
        return os;
	}
};


// [문제] 

//--------
int main()
//--------
{
    for (int i = 0; i < 10; ++i) {
        cout << Dog{} << endl;
    }

    

    save("메인.cpp");
}

==================================================
저장한 시간 - 2026-04-20 13:38:11.6527465
==================================================

//----------------------------------------------
// 2026 1학기 STL 월56 화 78
// 중간고사 4월 21일(화)
//----------------------------------------------
// STL 컨테이너 - Containers are objects that store other objects.
// Containers - 자료구조
// object - 메모리를 차지하는 객체(instancing)
//----------------------------------------------
#include <iostream>
#include <array>
#include "save.h"
#include "ZString.h"
using namespace std;

extern bool 관찰;     //  관찰하려면 true로


//--------
int main()
//--------
{

    array<ZString, 5> a{ "1", "22", "333", "4444", "55555"};
    
    // iterator로 a를 순회
    // element access - at, operator[], front, back, data

    
    cin >> a[0];
    관찰 = true;
    a[0].show();

    save("메인.cpp");
}

==================================================
저장한 시간 - 2026-04-20 13:47:57.2780170
==================================================

//----------------------------------------------
// 2026 1학기 STL 월56 화 78
// 중간고사 4월 21일(화)
//----------------------------------------------
// STL 컨테이너 - Containers are objects that store other objects.
// Containers - 자료구조
// object - 메모리를 차지하는 객체(instancing)
//----------------------------------------------
#include <iostream>
#include <array>
#include "save.h"
#include "ZString.h"
using namespace std;

extern bool 관찰;     //  관찰하려면 true로


//--------
int main()
//--------
{

    array<ZString, 5> a{ "1", "22", "333", "4444", "55555"};
    
    // array<ZString, 5>::iterator i = a.begin();
    auto i = a.begin(); // 위 코드와 같은 코드

    for (auto i = a.begin(); i != a.end(); ++i)
        cout << (*i).size() << endl;
    
    
    // iterator로 a를 순회
    // element access - at, operator[], front, back, data

    
    
    save("메인.cpp");
}

==================================================
저장한 시간 - 2026-04-20 13:48:03.7014724
==================================================

//----------------------------------------------
// 2026 1학기 STL 월56 화 78
// 중간고사 4월 21일(화)
//----------------------------------------------
// STL 컨테이너 - Containers are objects that store other objects.
// Containers - 자료구조
// object - 메모리를 차지하는 객체(instancing)
//----------------------------------------------
#include <iostream>
#include <array>
#include "save.h"
#include "ZString.h"
using namespace std;

extern bool 관찰;     //  관찰하려면 true로


//--------
int main()
//--------
{

    array<ZString, 5> a{ "1", "22", "333", "4444", "55555"};
    
    // array<ZString, 5>::iterator i = a.begin();
    auto i = a.begin(); // 위 코드와 같은 코드

    for (auto i = a.begin(); i != a.end(); ++i)
        cout << (*i) << endl;
    
    
    // iterator로 a를 순회
    // element access - at, operator[], front, back, data

    
    
    save("메인.cpp");
}

==================================================
저장한 시간 - 2026-04-20 13:50:42.3184958
==================================================

//----------------------------------------------
// 2026 1학기 STL 월56 화 78
// 중간고사 4월 21일(화)
//----------------------------------------------
// STL 컨테이너 - Containers are objects that store other objects.
// Containers - 자료구조
// object - 메모리를 차지하는 객체(instancing)
//----------------------------------------------
#include <iostream>
#include <array>
#include "save.h"
#include "ZString.h"
using namespace std;

extern bool 관찰;     //  관찰하려면 true로


//--------
int main()
//--------
{

    array<ZString, 5> a{ "1", "22", "333", "4444", "55555"};
    
    // iterator로 a를 순회
    // array<ZString, 5>::iterator i = a.begin();
    auto i = a.begin(); // 위 코드와 같은 코드

    for (auto i = a.rbegin(); i != a.rend(); ++i)
        cout << (*i) << endl;
    
    
    // element access - at, operator[], front, back, data

    
    
    save("메인.cpp");
}

==================================================
저장한 시간 - 2026-04-20 13:52:50.6528316
==================================================

//----------------------------------------------
// 2026 1학기 STL 월56 화 78
// 중간고사 4월 21일(화)
//----------------------------------------------
// STL 컨테이너 - Containers are objects that store other objects.
// Containers - 자료구조
// object - 메모리를 차지하는 객체(instancing)
//----------------------------------------------
#include <iostream>
#include <array>
#include "save.h"
#include "ZString.h"
using namespace std;

extern bool 관찰;     //  관찰하려면 true로


//--------
int main()
//--------
{

    array<ZString, 5> a{ "1", "22", "333", "4444", "55555"};
    
    // iterator로 a를 순회
    // array<ZString, 5>::iterator i = a.begin();
    auto i = a.begin(); // 위 코드와 같은 코드


    for (auto i = a.begin(); i != a.end(); ++i)
        cout << (*i) << endl;
    
    
    // element access - at, operator[], front, back, data
    관찰 = true;
    a[0].show();
    
    save("메인.cpp");
}

==================================================
저장한 시간 - 2026-04-20 13:53:30.7413224
==================================================

//----------------------------------------------
// 2026 1학기 STL 월56 화 78
// 중간고사 4월 21일(화)
//----------------------------------------------
// STL 컨테이너 - Containers are objects that store other objects.
// Containers - 자료구조
// object - 메모리를 차지하는 객체(instancing)
//----------------------------------------------
#include <iostream>
#include <array>
#include "save.h"
#include "ZString.h"
using namespace std;

extern bool 관찰;     //  관찰하려면 true로


//--------
int main()
//--------
{

    array<ZString, 5> a{ "1", "22", "333", "4444", "55555"};
    
    // iterator로 a를 순회
    // array<ZString, 5>::iterator i = a.begin();
    auto i = a.begin(); // 위 코드와 같은 코드


    for (auto i = a.begin(); i != a.end(); ++i)
        cout << (*i) << endl;
    
    
    // element access - at, operator[], front, back, data
    관찰 = true;
    a[0].show();
    a.front().show();   // a의 첫원소
    a.data()->show();   // a의 시작주소의 값을 show
    
    save("메인.cpp");
}

==================================================
저장한 시간 - 2026-04-20 13:54:06.4850565
==================================================

//----------------------------------------------
// 2026 1학기 STL 월56 화 78
// 중간고사 4월 21일(화)
//----------------------------------------------
// STL 컨테이너 - Containers are objects that store other objects.
// Containers - 자료구조
// object - 메모리를 차지하는 객체(instancing)
//----------------------------------------------
#include <iostream>
#include <array>
#include "save.h"
#include "ZString.h"
using namespace std;

extern bool 관찰;     //  관찰하려면 true로


//--------
int main()
//--------
{

    array<ZString, 5> a{ "1", "22", "333", "4444", "55555"};
    
    // iterator로 a를 순회
    // array<ZString, 5>::iterator i = a.begin();
    auto i = a.begin(); // 위 코드와 같은 코드


    for (auto i = a.begin(); i != a.end(); ++i)
        cout << (*i) << endl;
    
    
    // element access - at, operator[], front, back, data
    관찰 = true;
    // 4개 다 동일한 결과가 출력됨.
    a.begin()->show();
    a[0].show();
    a.front().show();   // a의 첫원소
    a.data()->show();   // a의 시작주소의 값을 show
    
    save("메인.cpp");
}

==================================================
저장한 시간 - 2026-04-20 13:55:57.5951496
==================================================

//----------------------------------------------
// 2026 1학기 STL 월56 화 78
// 중간고사 4월 21일(화)
//----------------------------------------------
// STL 컨테이너 - Containers are objects that store other objects.
// Containers - 자료구조
// object - 메모리를 차지하는 객체(instancing)
//----------------------------------------------
#include <iostream>
#include <array>
#include "save.h"
#include "ZString.h"
using namespace std;

extern bool 관찰;     //  관찰하려면 true로


//--------
int main()
//--------
{

    array<ZString, 5> a{ "1", "22", "333", "4444", "55555"};
    
    // iterator로 a를 순회
    // array<ZString, 5>::iterator i = a.begin();
    auto i = a.begin(); // 위 코드와 같은 코드


    for (auto i = a.begin(); i != a.end(); ++i)
        cout << (*i) << endl;
    
    
    // element access - at, operator[], front, back, data
    관찰 = true;
    // 4개 다 동일한 결과가 출력됨. - 첫원소에 access
    a.begin()->show();
    a[0].show();
    a.front().show();   // a의 첫원소
    a.data()->show();   // a의 시작주소의 값을 show

    // 마지막 원소에 access
    (a.end() - 1)->show();
    a[a.size()-1].show();
    a.back().show();
    (a.data() + a.size() - 1)->show();
    관찰 = false;
    
    save("메인.cpp");
}

==================================================
저장한 시간 - 2026-04-20 14:21:35.2759267
==================================================

//----------------------------------------------
// 2026 1학기 STL 월56 화 78
// 중간고사 4월 21일(화)
//----------------------------------------------
// STL 컨테이너 - Containers are objects that store other objects.
// Sequence Container
// - array<T, N> - 유일하게 컴파일 타임에 size 결정 - STACK, DATA
// - vector<T> - Dynamic (size) Array - free-store에 data관리
//----------------------------------------------
#include <iostream>
#include <vector>
#include <string>
#include <fstream>
#include <algorithm>
#include "save.h"
#include "ZString.h"
using namespace std;

extern bool 관찰;     //  관찰하려면 true로


//--------
int main()
//--------
{
    vector<ZString> v;
    
    // [문제] "메인.cpp"에 있는 모든 문자를 v에 저장하라.
    ifstream in{ "메인.cpp" };
    if (not in) return 0;
    
    ZString zs;
    while (in >> zs)
        v.push_back(zs);

    sort(v.begin(), v.end(), [](const ZString& a, const ZString& b) {
            return a.size() < b.size();
        });

    for (const ZString& zs : v)
        cout << zs << endl;

    save("메인.cpp");
}

==================================================
저장한 시간 - 2026-04-20 14:46:31.4897550
==================================================

//----------------------------------------------
// 2026 1학기 STL 월56 화 78
// 중간고사 4월 21일(화)
//----------------------------------------------
// STL 컨테이너 - Containers are objects that store other objects.
// Sequence Container
// - array<T, N> - 유일하게 컴파일 타임에 size 결정 - STACK, DATA
// - vector<T> - Dynamic (size) Array - free-store에 data관리
//----------------------------------------------
#include <iostream>
#include <vector>
#include <string>
#include <fstream>
#include <algorithm>
#include "save.h"
#include "ZString.h"
using namespace std;

extern bool 관찰;     //  관찰하려면 true로


//--------
int main()
//--------
{
    vector<int> v{ 1,2,3,4,5 }; // free-store

    cout << sizeof(v) << endl;
    cout << addressof(v) << endl;
    cout << typeid(v).name() << endl;
    

    save("메인.cpp");
}

==================================================
저장한 시간 - 2026-04-20 14:47:38.8490457
==================================================

//----------------------------------------------
// 2026 1학기 STL 월56 화 78
// 중간고사 4월 21일(화)
//----------------------------------------------
// STL 컨테이너 - Containers are objects that store other objects.
// Sequence Container
// - array<T, N> - 유일하게 컴파일 타임에 size 결정 - STACK, DATA
// - vector<T> - Dynamic (size) Array - free-store에 data관리
//----------------------------------------------
#include <iostream>
#include <vector>
#include <string>
#include <fstream>
#include <algorithm>
#include "save.h"
#include "ZString.h"
using namespace std;

extern bool 관찰;     //  관찰하려면 true로


//--------
int main()
//--------
{
    vector<int> v{ 1,2,3,4,5 }; // free-store

    cout << sizeof(v) << endl;          // 24byte
    cout << addressof(v) << endl;       // STACK
    cout << typeid(v).name() << endl;   // class std::vector<int,class std::allocator<int> >

    cout << "v가 저장할 수 있는 최대 int 개수 - " << v.max_size() << endl;
    

    save("메인.cpp");
}

==================================================
저장한 시간 - 2026-04-20 15:06:21.7177947
==================================================

//----------------------------------------------
// 2026 1학기 STL 월56 화 78
// 중간고사 4월 21일(화)
//----------------------------------------------
// STL 컨테이너 - Containers are objects that store other objects.
// Sequence Container
// - array<T, N> - 유일하게 컴파일 타임에 size 결정 - STACK, DATA
// - vector<T> - Dynamic (size) Array - free-store에 data관리
//----------------------------------------------
#include <iostream>
#include <vector>
#include <string>
#include <algorithm>
#include "save.h"
#include "ZString.h"
using namespace std;

extern bool 관찰;     //  관찰하려면 true로


//--------
int main()
//--------
{
    // [문제] 키보드에서 입력한 모든 단어를 저장하라.
    // 입력의 끝은 OS마다 따로 지정되어 있다. (ctrl + z)
    // 오름차순 정렬하고 출력하라.

    vector<string> v;

    string s;
    while (cin >> s) {
        v.push_back(s);
    }
    for (const string& s : v)
        cout << s << endl;
   
    // sort(v.begin(), v.end());


    save("메인.cpp");
}

==================================================
저장한 시간 - 2026-04-20 15:07:50.7531944
==================================================

//----------------------------------------------
// 2026 1학기 STL 월56 화 78
// 중간고사 4월 21일(화)
//----------------------------------------------
// STL 컨테이너 - Containers are objects that store other objects.
// Sequence Container
// - array<T, N> - 유일하게 컴파일 타임에 size 결정 - STACK, DATA
// - vector<T> - Dynamic (size) Array - free-store에 data관리
//----------------------------------------------
#include <iostream>
#include <vector>
#include <string>
#include <algorithm>
#include "save.h"
#include "ZString.h"
using namespace std;

extern bool 관찰;     //  관찰하려면 true로


//--------
int main()
//--------
{
    // [문제] 키보드에서 입력한 모든 단어를 저장하라.
    // 입력의 끝은 OS마다 따로 지정되어 있다. (ctrl + z)
    // 오름차순 정렬하고 출력하라.

    vector<string> v;

    string s;
    while (cin >> s) {
        v.push_back(s);
    }
   
    // 오름차순(ascending order) 정렬
    sort(v.begin(), v.end());

    for (const string& s : v)
        cout << s << endl;

    save("메인.cpp");
}

==================================================
저장한 시간 - 2026-04-20 15:10:16.7919845
==================================================

//----------------------------------------------
// 2026 1학기 STL 월56 화 78
// 중간고사 4월 21일(화)
//----------------------------------------------
// STL 컨테이너 - Containers are objects that store other objects.
// Sequence Container
// - array<T, N> - 유일하게 컴파일 타임에 size 결정 - STACK, DATA
// - vector<T> - Dynamic (size) Array - free-store에 data관리
//----------------------------------------------
#include <iostream>
#include <vector>
#include <string>
#include <algorithm>
#include "save.h"
#include "ZString.h"
using namespace std;

extern bool 관찰;     //  관찰하려면 true로


//--------
int main()
//--------
{
    // [문제] 키보드에서 입력한 모든 단어를 저장하라.
    // 입력의 끝은 OS마다 따로 지정되어 있다. (ctrl + z)
    // 오름차순 정렬하고 출력하라.

    vector<string> v;

    string s;
    while (cin >> s) {
        v.push_back(s);
    }
   
    // 오름차순(ascending order) 정렬
    sort(v.begin(), v.end());

    for (const string& s : v)
        cout << s << endl;

    save("메인.cpp");
}

==================================================
저장한 시간 - 2026-04-20 15:11:14.5563099
==================================================

//----------------------------------------------
// 2026 1학기 STL 월56 화 78
// 중간고사 4월 21일(화)
//----------------------------------------------
// STL 컨테이너 - Containers are objects that store other objects.
// Sequence Container
// - array<T, N> - 유일하게 컴파일 타임에 size 결정 - STACK, DATA
// - vector<T> - Dynamic (size) Array - free-store에 data관리
//----------------------------------------------
#include <iostream>
#include <vector>
#include <string>
#include <algorithm>
#include "save.h"
#include "ZString.h"
using namespace std;

extern bool 관찰;     //  관찰하려면 true로


//--------
int main()
//--------
{
    // [문제] 키보드에서 입력한 모든 단어를 저장하라.
    // 입력의 끝은 OS마다 따로 지정되어 있다. (ctrl + z)
    // 오름차순 정렬하고 출력하라.

    vector<string> v;

    string s;
    while (cin >> s) {
        v.push_back(s);
    }
   
    // 오름차순(ascending order) 정렬
    sort(v.begin(), v.end());

    for (auto i = v.rbegin(); i != v.rend(); ++i)
        cout << (*i) << endl;

    //for (const string& s : v)
    //    cout << s << endl;

    save("메인.cpp");
}

==================================================
저장한 시간 - 2026-04-20 15:14:20.0219977
==================================================

//----------------------------------------------
// 2026 1학기 STL 월56 화 78
// 중간고사 4월 21일(화)
//----------------------------------------------
// STL 컨테이너 - Containers are objects that store other objects.
// Sequence Container
// - array<T, N> - 유일하게 컴파일 타임에 size 결정 - STACK, DATA
// - vector<T> - Dynamic (size) Array - free-store에 data관리
//----------------------------------------------
#include <iostream>
#include <vector>
#include <string>
#include <algorithm>
#include "save.h"
#include "ZString.h"
using namespace std;

extern bool 관찰;     //  관찰하려면 true로


//--------
int main()
//--------
{
    // [문제] 키보드에서 입력한 모든 단어를 저장하라.
    // 입력의 끝은 OS마다 따로 지정되어 있다. (ctrl + z)
    // 오름차순 정렬하고 출력하라.

    vector<string> v{ istream_iterator<string>{cin}, {} };

   
    // 오름차순(ascending order) 정렬
    sort(v.begin(), v.end());

    for (const string& s : v)
        cout << s << endl;

    //for (auto i = v.rbegin(); i != v.rend(); ++i)
    //    cout << (*i) << endl;


    save("메인.cpp");
}

==================================================
저장한 시간 - 2026-04-20 15:17:37.4609985
==================================================

//----------------------------------------------
// 2026 1학기 STL 월56 화 78
// 중간고사 4월 21일(화)
//----------------------------------------------
// STL 컨테이너 - Containers are objects that store other objects.
// Sequence Container
// - array<T, N> - 유일하게 컴파일 타임에 size 결정 - STACK, DATA
// - vector<T> - Dynamic (size) Array - free-store에 data관리
//----------------------------------------------
#include <iostream>
#include <vector>
#include <algorithm>
#include <numeric>
#include "save.h"
#include "ZString.h"
using namespace std;

extern bool 관찰;     //  관찰하려면 true로


//--------
int main()
//--------
{
    // [문제] 키보드에서 입력한 모든 정수의 합계를 출력하라.
    vector<int> v{ istream_iterator<int>{cin}, {} };

    long long sum = accumulate(v.begin(), v.end(), 0LL);

    cout << sum << endl;


    save("메인.cpp");
}

==================================================
저장한 시간 - 2026-04-20 15:18:50.0289823
==================================================

//----------------------------------------------
// 2026 1학기 STL 월56 화 78
// 중간고사 4월 21일(화)
//----------------------------------------------
// STL 컨테이너 - Containers are objects that store other objects.
// Sequence Container
// - array<T, N> - 유일하게 컴파일 타임에 size 결정 - STACK, DATA
// - vector<T> - Dynamic (size) Array - free-store에 data관리
//----------------------------------------------
#include <iostream>
#include <vector>
#include <algorithm>
#include <numeric>
#include "save.h"
#include "ZString.h"
using namespace std;

extern bool 관찰;     //  관찰하려면 true로


//--------
int main()
//--------
{
    // [문제] 키보드에서 입력한 모든 정수의 합계를 출력하라.

    cout << accumulate(istream_iterator<int>{cin}, {}, 0LL) << endl;


    save("메인.cpp");
}

==================================================
저장한 시간 - 2026-04-20 15:19:15.3735668
==================================================

//----------------------------------------------
// 2026 1학기 STL 월56 화 78
// 중간고사 4월 21일(화)
//----------------------------------------------
// STL 컨테이너 - Containers are objects that store other objects.
// Sequence Container
// - array<T, N> - 유일하게 컴파일 타임에 size 결정 - STACK, DATA
// - vector<T> - Dynamic (size) Array - free-store에 data관리
//----------------------------------------------
#include <iostream>
#include <vector>
#include <algorithm>
#include <numeric>
#include "save.h"
#include "ZString.h"
using namespace std;

extern bool 관찰;     //  관찰하려면 true로


//--------
int main()
//--------
{
    // [문제] 키보드에서 입력한 모든 정수의 합계를 출력하라.

    cout << "합계 - " << accumulate(istream_iterator<int>{cin}, {}, 0LL) << endl;


    save("메인.cpp");
}

==================================================
저장한 시간 - 2026-04-27 13:39:31.2295077
==================================================

//----------------------------------------------
// 2026 1학기 STL 월56 화 78
// 중간고사 4월 21일(화)
//----------------------------------------------
// STL 컨테이너 - Containers are objects that store other objects.
// Sequence Container
// - array<T, N> - 유일하게 컴파일 타임에 size 결정 - STACK, DATA
// - vector<T> - Dynamic (size) Array - free-store에 data관리
//----------------------------------------------
#include <iostream>
#include <vector>
#include <algorithm>
#include <numeric>
#include "save.h"
#include "ZString.h"
using namespace std;

extern bool 관찰;     //  관찰하려면 true로


//--------
int main()
//--------
{
    vector<ZString> v{ "1", "22", "333", "4444", "55555" };

    cout << endl;
    cout << "v의 원소에 접근" << endl;

    관찰 = true;
    for (ZString zs : v)
        cout << zs << endl;
    관찰 = false;


    save("메인.cpp");
}

==================================================
저장한 시간 - 2026-04-27 13:39:45.3621201
==================================================

//----------------------------------------------
// 2026 1학기 STL 월56 화 78
// 중간고사 4월 21일(화)
//----------------------------------------------
// STL 컨테이너 - Containers are objects that store other objects.
// Sequence Container
// - array<T, N> - 유일하게 컴파일 타임에 size 결정 - STACK, DATA
// - vector<T> - Dynamic (size) Array - free-store에 data관리
//----------------------------------------------
#include <iostream>
#include <vector>
#include <algorithm>
#include <numeric>
#include "save.h"
#include "ZString.h"
using namespace std;

extern bool 관찰;     //  관찰하려면 true로


//--------
int main()
//--------
{
    vector<ZString> v{ "1", "22", "333", "4444", "55555" };

    cout << endl;
    cout << "v의 원소에 접근" << endl;

    관찰 = true;
    for (ZString& zs : v)
        cout << zs << endl;
    관찰 = false;


    save("메인.cpp");
}

==================================================
저장한 시간 - 2026-04-27 13:42:42.5465082
==================================================

//----------------------------------------------
// 2026 1학기 STL 월56 화 78
// 중간고사 4월 21일(화)
//----------------------------------------------
// STL 컨테이너 - Containers are objects that store other objects.
// Sequence Container
// - array<T, N> - 유일하게 컴파일 타임에 size 결정 - STACK, DATA
// - vector<T> - Dynamic (size) Array - free-store에 data관리
//----------------------------------------------
#include <iostream>
#include <vector>
#include <algorithm>
#include <numeric>
#include "save.h"
#include "ZString.h"
using namespace std;

extern bool 관찰;     //  관찰하려면 true로


//--------
int main()
//--------
{
    vector<ZString> v{ "1", "22", "333", "4444", "55555" };

    관찰 = true;
    v.push_back("4444");
    관찰 = false;


    cout << endl;
    cout << "v의 원소에 접근" << endl;

    관찰 = true;
    for (ZString& zs : v)
        cout << zs << endl;
    관찰 = false;


    save("메인.cpp");
}

==================================================
저장한 시간 - 2026-04-27 13:43:09.8321007
==================================================

//----------------------------------------------
// 2026 1학기 STL 월56 화 78
// 중간고사 4월 21일(화)
//----------------------------------------------
// STL 컨테이너 - Containers are objects that store other objects.
// Sequence Container
// - array<T, N> - 유일하게 컴파일 타임에 size 결정 - STACK, DATA
// - vector<T> - Dynamic (size) Array - free-store에 data관리
//----------------------------------------------
#include <iostream>
#include <vector>
#include <algorithm>
#include <numeric>
#include "save.h"
#include "ZString.h"
using namespace std;

extern bool 관찰;     //  관찰하려면 true로


//--------
int main()
//--------
{
    vector<ZString> v{ "1", "22", "333" };

    관찰 = true;
    v.push_back("4444");
    관찰 = false;


    cout << endl;
    cout << "v의 원소에 접근" << endl;


    for (ZString& zs : v)
        cout << zs << endl;

    save("메인.cpp");
}

==================================================
저장한 시간 - 2026-04-27 13:58:18.7399704
==================================================

//----------------------------------------------
// 2026 1학기 STL 월56 화 78
// 중간고사 4월 21일(화)
//----------------------------------------------
// STL 컨테이너 - Containers are objects that store other objects.
// Sequence Container
// - array<T, N> - 유일하게 컴파일 타임에 size 결정 - STACK, DATA
// - vector<T> - Dynamic (size) Array - free-store에 data관리
//----------------------------------------------
#include <iostream>
#include <vector>
#include <algorithm>
#include <numeric>
#include "save.h"
#include "ZString.h"
using namespace std;

extern bool 관찰;     //  관찰하려면 true로


//--------
int main()
//--------
{
    vector<ZString> v{ "1", "22", "333" };

    관찰 = true;
    v.push_back("4444");
    v.push_back("55555");
    v.push_back("666666");
    관찰 = false;


    cout << endl;
    cout << "v의 원소에 접근" << endl;


    for (ZString& zs : v)
        cout << zs << endl;

    save("메인.cpp");
}

==================================================
저장한 시간 - 2026-04-27 13:58:40.7118830
==================================================

//----------------------------------------------
// 2026 1학기 STL 월56 화 78
// 중간고사 4월 21일(화)
//----------------------------------------------
// STL 컨테이너 - Containers are objects that store other objects.
// Sequence Container
// - array<T, N> - 유일하게 컴파일 타임에 size 결정 - STACK, DATA
// - vector<T> - Dynamic (size) Array - free-store에 data관리
//----------------------------------------------
#include <iostream>
#include <vector>
#include <algorithm>
#include <numeric>
#include "save.h"
#include "ZString.h"
using namespace std;

extern bool 관찰;     //  관찰하려면 true로


//--------
int main()
//--------
{
    vector<ZString> v{ "1", "22", "333" };
    v.reserve(10);  // 10개의 공간을 마련

    관찰 = true;
    v.push_back("4444");
    v.push_back("55555");
    v.push_back("666666");
    관찰 = false;


    cout << endl;
    cout << "v의 원소에 접근" << endl;


    for (ZString& zs : v)
        cout << zs << endl;

    save("메인.cpp");
}

==================================================
저장한 시간 - 2026-04-27 14:11:43.6252348
==================================================

//----------------------------------------------
// 2026 1학기 STL 월56 화 78
// 중간고사 4월 21일(화)
//----------------------------------------------
// STL 컨테이너 - Containers are objects that store other objects.
// Sequence Container
// - array<T, N> - 유일하게 컴파일 타임에 size 결정 - STACK, DATA
// - vector<T> - Dynamic (size) Array - free-store에 data관리
//----------------------------------------------
#include <iostream>
#include <vector>
#include <algorithm>
#include <numeric>
#include "save.h"
#include "ZString.h"
using namespace std;

extern bool 관찰;     //  관찰하려면 true로


//--------
int main()
//--------
{
    vector<ZString> v{ "1", "22", "333" };
    v.reserve(5);  // 10개의 공간을 마련

    관찰 = true;
    v.push_back("4444");        // Creator(생성자)가 아님.
    v.push_back("55555");
    // call by value - STACK에 생성
    // 생성자 - 만들어진 메모리에 색칠하는 놈?
    관찰 = false;


    cout << endl;
    cout << "v의 원소에 접근" << endl;


    for (ZString& zs : v)
        cout << zs << endl;

    save("메인.cpp");
}

==================================================
저장한 시간 - 2026-04-27 14:12:33.5026228
==================================================

//----------------------------------------------
// 2026 1학기 STL 월56 화 78
// 중간고사 4월 21일(화)
//----------------------------------------------
// STL 컨테이너 - Containers are objects that store other objects.
// Sequence Container
// - array<T, N> - 유일하게 컴파일 타임에 size 결정 - STACK, DATA
// - vector<T> - Dynamic (size) Array - free-store에 data관리
//----------------------------------------------
#include <iostream>
#include <vector>
#include <algorithm>
#include <numeric>
#include "save.h"
#include "ZString.h"
using namespace std;

extern bool 관찰;     //  관찰하려면 true로


//--------
int main()
//--------
{
    vector<ZString> v{ "1", "22", "333" };
    v.reserve(5);  // 10개의 공간을 마련

    관찰 = true;
    v.emplace_back("4444");        // Creator(생성자)가 아님.
    v.emplace_back("55555");
    // 생성자 - 만들어진 메모리에 색칠하는 놈?
    관찰 = false;


    cout << endl;
    cout << "v의 원소에 접근" << endl;


    for (ZString& zs : v)
        cout << zs << endl;

    save("메인.cpp");
}

==================================================
저장한 시간 - 2026-04-27 14:15:27.9148679
==================================================

//----------------------------------------------
// 2026 1학기 STL 월56 화 78
// 중간고사 4월 21일(화)
//----------------------------------------------
// STL 컨테이너 - Containers are objects that store other objects.
// Sequence Container
// - array<T, N> - 유일하게 컴파일 타임에 size 결정 - STACK, DATA
// - vector<T> - Dynamic (size) Array - free-store에 data관리
//----------------------------------------------
#include <iostream>
#include <vector>
#include <algorithm>
#include <numeric>
#include "save.h"
#include "ZString.h"
using namespace std;

extern bool 관찰;     //  관찰하려면 true로


//--------
int main()
//--------
{
    vector<ZString> v{ "1", "22", "333" };
    v.reserve(5);  // 10개의 공간을 마련

    관찰 = true;
    v.emplace_back("4444");        // Creator(생성자)가 아님.
    v.emplace_back( );      // 디폴트 생성자
    // emplace_back - 남은 메모리에 직접 색칠 (임시 객체를 생성하지 않음)
    // 생성자 - 만들어진 메모리에 색칠하는 놈?
    관찰 = false;


    cout << endl;
    cout << "v의 원소에 접근" << endl;


    for (ZString& zs : v)
        cout << zs << endl;

    save("메인.cpp");
}

==================================================
저장한 시간 - 2026-04-27 14:22:44.4016309
==================================================

//----------------------------------------------
// 2026 1학기 STL 월56 화 78
// 중간고사 4월 21일(화)
//----------------------------------------------
// STL 컨테이너 - Containers are objects that store other objects.
// Sequence Container
// - array<T, N> - 유일하게 컴파일 타임에 size 결정 - STACK, DATA
// - vector<T> - Dynamic (size) Array - free-store에 data관리
//----------------------------------------------
#include <iostream>
#include <vector>
#include <algorithm>
#include <numeric>
#include "save.h"
#include "ZString.h"
using namespace std;

extern bool 관찰;     //  관찰하려면 true로


//--------
int main()
//--------
{
    save("메인.cpp");

    // 벡터가 메모리를 확장하는 순간을 관찰
    vector<int> v;

    while (true) {
        v.push_back(1);
    

        if (v.size() == v.capacity()) {
            cout << "원소 개수 - " << v.size() << endl;
            cout << "확보한 개수 - " << v.capacity() << endl;
            cout << endl;
        }
    }

}

==================================================
저장한 시간 - 2026-04-27 14:28:42.4243478
==================================================

//----------------------------------------------
// 2026 1학기 STL 월56 화 78
// 중간고사 4월 21일(화)
//----------------------------------------------
// STL 컨테이너 - Containers are objects that store other objects.
// Sequence Container
// - array<T, N> - 유일하게 컴파일 타임에 size 결정 - STACK, DATA
// - vector<T> - Dynamic (size) Array - free-store에 data관리
//----------------------------------------------
#include <iostream>
#include <vector>
#include <algorithm>
#include <numeric>
#include "save.h"
#include "ZString.h"
using namespace std;

extern bool 관찰;     //  관찰하려면 true로


//--------
int main()
//--------
{
    save("메인.cpp");

    // 벡터가 메모리를 확장하는 순간을 관찰
    vector<int> v;

    while (true) {
        try {
            v.push_back(1);
        }
        catch (exception& e) {
            cout << "예외 발생 - " << e.what() << endl;
            cout << "현재 원소 개수 - " << v.size() << endl;
            cout << "현재 용량 - " << v.capacity() << endl;
        }
    }

}

==================================================
저장한 시간 - 2026-04-27 14:49:45.0322644
==================================================

//----------------------------------------------
// 2026 1학기 STL 월56 화 78
// 중간고사 4월 21일(화)
//----------------------------------------------
// STL 컨테이너 - Containers are objects that store other objects.
// Sequence Container
// - array<T, N> - 유일하게 컴파일 타임에 size 결정 - STACK, DATA
// - vector<T> - Dynamic (size) Array - free-store에 data관리
//----------------------------------------------
#include <iostream>
#include <vector>
#include <fstream>
#include "save.h"
#include "ZString.h"
using namespace std;

extern bool 관찰;     //  관찰하려면 true로


//--------
int main()
//--------
{
    save("메인.cpp");
    vector<int> v;
    v.reserve(26);

    // [문제] "메인.cpp"에 알파벳 소문자가 몇 개나 있는지 다음과 같이 출력하라.
    // a - 10
    // b - 5
    // 중간 생략
    // z - 1

    ifstream in{ "메인.cpp" };
    if (not in) return 2;

    char c;
    while (in >> c) {
        if (c >= 'a' && c <= 'z')
            v[c - 'a']++;
    }

    for (int num : v) {
        cout << num << endl;
    }
}

==================================================
저장한 시간 - 2026-04-27 14:55:53.1998699
==================================================

//----------------------------------------------
// 2026 1학기 STL 월56 화 78
// 중간고사 4월 21일(화)
//----------------------------------------------
// STL 컨테이너 - Containers are objects that store other objects.
// Sequence Container
// - array<T, N> - 유일하게 컴파일 타임에 size 결정 - STACK, DATA
// - vector<T> - Dynamic (size) Array - free-store에 data관리
//----------------------------------------------
#include <iostream>
#include <vector>
#include <array>
#include <fstream>
#include "save.h"
#include "ZString.h"
using namespace std;

extern bool 관찰;     //  관찰하려면 true로


//--------
int main()
//--------
{
    // [문제] "메인.cpp"에 알파벳 소문자가 몇 개나 있는지 다음과 같이 출력하라.
    // a - 10
    // b - 5
    // 중간 생략
    // z - 1

    // size_t alpha[26];
    array<int, 26> alpha;

    for (int i = 0; i < 26; ++i) {
        cout << static_cast<char>('a' + i) << " - " << alpha[i] << endl;
    }

     save("메인.cpp");
}

==================================================
저장한 시간 - 2026-04-27 14:57:48.8556037
==================================================

//----------------------------------------------
// 2026 1학기 STL 월56 화 78
// 중간고사 4월 21일(화)
//----------------------------------------------
// STL 컨테이너 - Containers are objects that store other objects.
// Sequence Container
// - array<T, N> - 유일하게 컴파일 타임에 size 결정 - STACK, DATA
// - vector<T> - Dynamic (size) Array - free-store에 data관리
//----------------------------------------------
#include <iostream>
#include <vector>
#include <array>
#include <fstream>
#include "save.h"
#include "ZString.h"
using namespace std;

extern bool 관찰;     //  관찰하려면 true로


//--------
int main()
//--------
{
    // [문제] "메인.cpp"에 알파벳 소문자가 몇 개나 있는지 다음과 같이 출력하라.
    // a - 10
    // b - 5
    // 중간 생략
    // z - 1

    // size_t alpha[26];
    array<int, 26> alpha;

    ifstream in{ "메인.cpp" };
    if (not in) return 2;

    char c;
    while (in >> c) {
        if (c >= 'a' && c <= 'z')
            ++alpha[c - 'a'];
    }

    for (int i = 0; i < 26; ++i) {
        cout << static_cast<char>('a' + i) << " - " << alpha[i] << endl;
    }

     save("메인.cpp");
}

==================================================
저장한 시간 - 2026-04-27 14:58:34.9942358
==================================================

//----------------------------------------------
// 2026 1학기 STL 월56 화 78
// 중간고사 4월 21일(화)
//----------------------------------------------
// STL 컨테이너 - Containers are objects that store other objects.
// Sequence Container
// - array<T, N> - 유일하게 컴파일 타임에 size 결정 - STACK, DATA
// - vector<T> - Dynamic (size) Array - free-store에 data관리
//----------------------------------------------
#include <iostream>
#include <vector>
#include <array>
#include <fstream>
#include "save.h"
#include "ZString.h"
using namespace std;

extern bool 관찰;     //  관찰하려면 true로


//--------
int main()
//--------
{
    // [문제] "메인.cpp"에 알파벳 소문자가 몇 개나 있는지 다음과 같이 출력하라.
    // a - 10
    // b - 5
    // 중간 생략
    // z - 1

    // size_t alpha[26];
    array<int, 26> alpha;
    alpha.fill(0);

    ifstream in{ "메인.cpp" };
    if (not in) return 2;

    char c;
    while (in >> c) {
        if (islower(c))
            ++alpha[c - 'a'];
    }

    for (int i = 0; i < 26; ++i) {
        cout << static_cast<char>('a' + i) << " - " << alpha[i] << endl;
    }

     save("메인.cpp");
}

==================================================
저장한 시간 - 2026-04-27 14:59:45.4423699
==================================================

//----------------------------------------------
// 2026 1학기 STL 월56 화 78
// 중간고사 4월 21일(화)
//----------------------------------------------
// STL 컨테이너 - Containers are objects that store other objects.
// Sequence Container
// - array<T, N> - 유일하게 컴파일 타임에 size 결정 - STACK, DATA
// - vector<T> - Dynamic (size) Array - free-store에 data관리
//----------------------------------------------
#include <iostream>
#include <vector>
#include <array>
#include <fstream>
#include "save.h"
#include "ZString.h"
using namespace std;

extern bool 관찰;     //  관찰하려면 true로


//--------
int main()
//--------
{
    // [문제] "메인.cpp"에 알파벳 소문자가 몇 개나 있는지 다음과 같이 출력하라.
    // a - 10
    // b - 5
    // 중간 생략
    // z - 1

    // size_t alpha[26];
    array<int, 26> alpha;
    alpha.fill(0);

    ifstream in{ "메인.cpp" };
    if (not in) return 2;

    char c;
    while (in >> c) {
        if (islower(c))
            ++alpha[c - 'a'];
    }

    for (int i = 0; i < 26; ++i) {
        cout << static_cast<char>('a' + i) << " - " << alpha[i] << endl;
    }

     save("메인.cpp");
}

==================================================
저장한 시간 - 2026-04-27 15:00:52.5398047
==================================================

//----------------------------------------------
// 2026 1학기 STL 월56 화 78
// 중간고사 4월 21일(화)
//----------------------------------------------
// STL 컨테이너 - Containers are objects that store other objects.
// Sequence Container
// - array<T, N> - 유일하게 컴파일 타임에 size 결정 - STACK, DATA
// - vector<T> - Dynamic (size) Array - free-store에 data관리
//----------------------------------------------
#include <iostream>
#include <vector>
#include <array>
#include <fstream>
#include "save.h"
#include "ZString.h"
using namespace std;

extern bool 관찰;     //  관찰하려면 true로


//--------
int main()
//--------
{
    // [문제] "메인.cpp"에 알파벳 소문자가 몇 개나 있는지 다음과 같이 출력하라.
    // a - 10
    // b - 5
    // 중간 생략
    // z - 1
    // zzzzzzzzzz

    // size_t alpha[26];
    array<int, 26> alpha;
    alpha.fill(0);

    ifstream in{ "메인.cpp" };
    if (not in) return 2;

    char c;
    while (in >> c) {
        if (islower(c))
            ++alpha[c - 'a'];
    }

    for (int i = 0; i < 26; ++i) {
        cout << static_cast<char>('a' + i) << " - " << alpha[i] << endl;
    }

     save("메인.cpp");
}

==================================================
저장한 시간 - 2026-04-27 15:03:09.0998113
==================================================

//----------------------------------------------
// 2026 1학기 STL 월56 화 78
// 중간고사 4월 21일(화)
//----------------------------------------------
// STL 컨테이너 - Containers are objects that store other objects.
// Sequence Container
// - array<T, N> - 유일하게 컴파일 타임에 size 결정 - STACK, DATA
// - vector<T> - Dynamic (size) Array - free-store에 data관리
//----------------------------------------------
#include <iostream>
#include <vector>
#include <array>
#include <fstream>
#include "save.h"
#include "ZString.h"
using namespace std;

extern bool 관찰;     //  관찰하려면 true로


//--------
int main()
//--------
{
    // [문제] "메인.cpp"에 알파벳 소문자가 몇 개나 있는지 다음과 같이 출력하라.
    // a - 10
    // b - 5
    // 중간 생략
    // z - 1

    // size_t alpha[26];
    array<int, 26> alpha{};

    ifstream in{ "메인.cpp" };
    if (not in) return 2;

    char c;
    while (in >> c) {
        if (islower(c))
            ++alpha[c - 'a'];
    }

    for (int i = 0; i < 26; ++i) {
        cout << static_cast<char>('a' + i) << " - " << alpha[i] << endl;
    }

     save("메인.cpp");
}

==================================================
저장한 시간 - 2026-04-27 15:09:07.6174931
==================================================

//----------------------------------------------
// 2026 1학기 STL 월56 화 78
// 중간고사 4월 21일(화)
//----------------------------------------------
// STL 컨테이너 - Containers are objects that store other objects.
// Sequence Container
// - array<T, N> - 유일하게 컴파일 타임에 size 결정 - STACK, DATA
// - vector<T> - Dynamic (size) Array - free-store에 data관리
//----------------------------------------------
#include <iostream>
#include <vector>
#include <algorithm>
#include "save.h"
#include "ZString.h"
using namespace std;

extern bool 관찰;     //  관찰하려면 true로


//--------
int main()
//--------
{
    vector<int> v{ 1,2,3,4,5 };

    // [문제] v에서 3을 제거하라.
    remove(v.begin(), v.end(), 3);

     save("메인.cpp");
}

==================================================
저장한 시간 - 2026-04-27 15:09:44.5515452
==================================================

//----------------------------------------------
// 2026 1학기 STL 월56 화 78
// 중간고사 4월 21일(화)
//----------------------------------------------
// STL 컨테이너 - Containers are objects that store other objects.
// Sequence Container
// - array<T, N> - 유일하게 컴파일 타임에 size 결정 - STACK, DATA
// - vector<T> - Dynamic (size) Array - free-store에 data관리
//----------------------------------------------
#include <iostream>
#include <vector>
#include <algorithm>
#include "save.h"
#include "ZString.h"
using namespace std;

extern bool 관찰;     //  관찰하려면 true로


//--------
int main()
//--------
{
    vector<int> v{ 1,2,3,4,5 };

    // [문제] v에서 3을 제거하라.
    remove(v.begin(), v.end(), 3);

    for (int num : v)
        cout << num << endl;

     save("메인.cpp");
}

==================================================
저장한 시간 - 2026-04-27 15:11:31.0967733
==================================================

//----------------------------------------------
// 2026 1학기 STL 월56 화 78
// 중간고사 4월 21일(화)
//----------------------------------------------
// STL 컨테이너 - Containers are objects that store other objects.
// Sequence Container
// - array<T, N> - 유일하게 컴파일 타임에 size 결정 - STACK, DATA
// - vector<T> - Dynamic (size) Array - free-store에 data관리
//----------------------------------------------
#include <iostream>
#include <vector>
#include <algorithm>
#include "save.h"
#include "ZString.h"
using namespace std;

extern bool 관찰;     //  관찰하려면 true로


//--------
int main()
//--------
{
    vector<int> v{ 1,2,3,4,5 };

    // [문제] v에서 3을 제거하라.
    remove(v.begin(), v.end(), 3);
    v.resize(4);

    for (int num : v)
        cout << num << endl;

     save("메인.cpp");
}

==================================================
저장한 시간 - 2026-04-28 16:17:34.2968720
==================================================

//----------------------------------------------
// 2026 1학기 STL 월56 화 78
// 중간고사 4월 21일(화)
//----------------------------------------------
// STL 컨테이너 - Containers are objects that store other objects.
// Sequence Container
// - array<T, N> - 유일하게 컴파일 타임에 size 결정 - STACK, DATA
// - vector<T> - Dynamic (size) Array - free-store에 data관리
//----------------------------------------------
#include <iostream>
#include <vector>
#include <algorithm>
#include "save.h"
#include "ZString.h"
using namespace std;

extern bool 관찰;     //  관찰하려면 true로


//--------
int main()
//--------
{
    vector<int> v{ 1,2,3,4,5 };

    // [문제] v에서 3을 제거하라.
    // v.size == 4, v.capacity == 5
    v.erase(remove(v.begin(), v.end(), 3), v.end());

    for (int num : v)
        cout << num << endl;

     save("메인.cpp");
}

==================================================
저장한 시간 - 2026-04-28 16:41:07.6155369
==================================================

//----------------------------------------------
// 2026 1학기 STL 월56 화 78
// 중간고사 4월 21일(화)
//----------------------------------------------
// STL 컨테이너 - Containers are objects that store other objects.
// Sequence Container
// - array<T, N> - 유일하게 컴파일 타임에 size 결정 - STACK, DATA
// - vector<T> - Dynamic (size) Array - free-store에 data관리
//----------------------------------------------
#include <iostream>
#include <vector>
#include <algorithm>
#include "save.h"
#include "ZString.h"
using namespace std;

extern bool 관찰;     //  관찰하려면 true로


//--------
int main()
//--------
{
    vector<int> v{ 1,2,3,4,5 };

    // [문제] v에서 3을 제거하라.
    // v.size == 4, v.capacity == 5
    erase(v, 3);

    for (int num : v)
        cout << num << endl;

     save("메인.cpp");
}

==================================================
저장한 시간 - 2026-04-28 17:03:21.1511728
==================================================

//----------------------------------------------
// 2026 1학기 STL 월56 화 78
// 중간고사 4월 21일(화)
//----------------------------------------------
// STL 컨테이너 - Containers are objects that store other objects.
// Sequence Container
// - array<T, N> - 유일하게 컴파일 타임에 size 결정 - STACK, DATA
// - vector<T> - Dynamic (size) Array - free-store에 data관리
//----------------------------------------------
#include <iostream>
#include <vector>
#include <algorithm>
#include "save.h"
#include "ZString.h"
using namespace std;

extern bool 관찰;     //  관찰하려면 true로


//--------
int main()
//--------
{
    vector<ZString> v{ "1", "22", "333"};

    // [문제] v에서 "22"를 제거하라
    관찰 = true;
    remove(v.begin(), v.end(), "22");
    관찰 = false;

    for (int i = 0; i < v.size(); ++i)
        cout << v[i] << endl;

     save("메인.cpp");
}

==================================================
저장한 시간 - 2026-04-28 17:05:16.1589562
==================================================

//----------------------------------------------
// 2026 1학기 STL 월56 화 78
// 중간고사 4월 21일(화)
//----------------------------------------------
// STL 컨테이너 - Containers are objects that store other objects.
// Sequence Container
// - array<T, N> - 유일하게 컴파일 타임에 size 결정 - STACK, DATA
// - vector<T> - Dynamic (size) Array - free-store에 data관리
//----------------------------------------------
#include <iostream>
#include <vector>
#include <algorithm>
#include "save.h"
#include "ZString.h"
using namespace std;

extern bool 관찰;     //  관찰하려면 true로


//--------
int main()
//--------
{
    vector<ZString> v{ "1", "22", "333"};

    // [문제] v에서 "22"를 제거하라
    remove(v.begin(), v.end(), "22");

    관찰 = true;
    for (int i = 0; i < v.size(); ++i)
        v[i].show();
    관찰 = false;

     save("메인.cpp");
}

==================================================
저장한 시간 - 2026-04-28 17:10:08.3257511
==================================================

//----------------------------------------------
// 2026 1학기 STL 월56 화 78
// 중간고사 4월 21일(화)
//----------------------------------------------
// STL 컨테이너 - Containers are objects that store other objects.
// Sequence Container
// - array<T, N> - 유일하게 컴파일 타임에 size 결정 - STACK, DATA
// - vector<T> - Dynamic (size) Array - free-store에 data관리
//----------------------------------------------
#include <iostream>
#include <vector>
#include <algorithm>
#include "save.h"
#include "ZString.h"
using namespace std;

extern bool 관찰;     //  관찰하려면 true로


//--------
int main()
//--------
{
    vector<ZString> v{ "1", "22", "4444", "55555"};

    // [문제] "22"다음에 "333"을 추가하라
    v.insert(v.begin() + 2, "333");


    for (const ZString& zs : v)
        cout << zs << endl;
    
    

     save("메인.cpp");
}

==================================================
저장한 시간 - 2026-04-28 17:13:04.3088100
==================================================

//----------------------------------------------
// 2026 1학기 STL 월56 화 78
// 중간고사 4월 21일(화)
//----------------------------------------------
// STL 컨테이너 - Containers are objects that store other objects.
// Sequence Container
// - array<T, N> - 유일하게 컴파일 타임에 size 결정 - STACK, DATA
// - vector<T> - Dynamic (size) Array - free-store에 data관리
//----------------------------------------------
#include <iostream>
#include <vector>
#include <algorithm>
#include "save.h"
#include "ZString.h"
using namespace std;

extern bool 관찰;     //  관찰하려면 true로


//--------
int main()
//--------
{
    vector<ZString> v{ "1", "22", "4444", "55555"};

    // [문제] "22"다음에 "333"을 추가하라
    v.insert(++++v.begin(), "333");


    for (const ZString& zs : v)
        cout << zs << endl;

     save("메인.cpp");
}

==================================================
저장한 시간 - 2026-04-28 17:14:23.0932233
==================================================

//----------------------------------------------
// 2026 1학기 STL 월56 화 78
// 중간고사 4월 21일(화)
//----------------------------------------------
// STL 컨테이너 - Containers are objects that store other objects.
// Sequence Container
// - array<T, N> - 유일하게 컴파일 타임에 size 결정 - STACK, DATA
// - vector<T> - Dynamic (size) Array - free-store에 data관리
//----------------------------------------------
#include <iostream>
#include <vector>
#include <algorithm>
#include "save.h"
#include "ZString.h"
using namespace std;

extern bool 관찰;     //  관찰하려면 true로


//--------
int main()
//--------
{
    vector<ZString> v{ "1", "22", "4444", "55555"};

    // [문제] "22"다음에 "333"을 추가하라
    관찰 = true;
    v.insert(++++v.begin(), "333");
    관찰 = false;

    for (const ZString& zs : v)
        cout << zs << endl;

     save("메인.cpp");
}

==================================================
저장한 시간 - 2026-04-28 17:14:45.1787399
==================================================

//----------------------------------------------
// 2026 1학기 STL 월56 화 78
// 중간고사 4월 21일(화)
//----------------------------------------------
// STL 컨테이너 - Containers are objects that store other objects.
// Sequence Container
// - array<T, N> - 유일하게 컴파일 타임에 size 결정 - STACK, DATA
// - vector<T> - Dynamic (size) Array - free-store에 data관리
//----------------------------------------------
#include <iostream>
#include <vector>
#include <algorithm>
#include "save.h"
#include "ZString.h"
using namespace std;

extern bool 관찰;     //  관찰하려면 true로


//--------
int main()
//--------
{
    vector<ZString> v{ "1", "22", "4444", "55555"};

    // [문제] "22"다음에 "333"을 추가하라
    관찰 = true;
    v.insert(v.begin() + 2, "333");
    관찰 = false;

    for (const ZString& zs : v)
        cout << zs << endl;

     save("메인.cpp");
}

==================================================
저장한 시간 - 2026-04-28 17:15:48.8986923
==================================================

//----------------------------------------------
// 2026 1학기 STL 월56 화 78
// 중간고사 4월 21일(화)
//----------------------------------------------
// STL 컨테이너 - Containers are objects that store other objects.
// Sequence Container
// - array<T, N> - 유일하게 컴파일 타임에 size 결정 - STACK, DATA
// - vector<T> - Dynamic (size) Array - free-store에 data관리
//----------------------------------------------
#include <iostream>
#include <vector>
#include <algorithm>
#include <list>
#include "save.h"
#include "ZString.h"
using namespace std;

extern bool 관찰;     //  관찰하려면 true로


//--------
int main()
//--------
{
    vector<ZString> v{ "1", "22", "4444", "55555"};

    // [문제] "22"다음에 "333"을 추가하라
    관찰 = true;
    v.insert(v.begin() + 2, "333"); // 전부 이동시켜 버림. 굉장히 비효율적
    관찰 = false;

    for (const ZString& zs : v)
        cout << zs << endl;

     save("메인.cpp");
}

==================================================
저장한 시간 - 2026-04-28 17:16:17.5520283
==================================================

//----------------------------------------------
// 2026 1학기 STL 월56 화 78
// 중간고사 4월 21일(화)
//----------------------------------------------
// STL 컨테이너 - Containers are objects that store other objects.
// Sequence Container
// - array<T, N> - 유일하게 컴파일 타임에 size 결정 - STACK, DATA
// - vector<T> - Dynamic (size) Array - free-store에 data관리
//----------------------------------------------
#include <iostream>
#include <vector>
#include <algorithm>
#include <list>
#include "save.h"
#include "ZString.h"
using namespace std;

extern bool 관찰;     //  관찰하려면 true로


//--------
int main()
//--------
{
    list<ZString> v{ "1", "22", "4444", "55555"};

    // [문제] "22"다음에 "333"을 추가하라
    관찰 = true;
    v.insert(++++v.begin(), "333"); // 전부 이동시켜 버림. 굉장히 비효율적
    관찰 = false;

    for (const ZString& zs : v)
        cout << zs << endl;

     save("메인.cpp");
}

==================================================
저장한 시간 - 2026-04-28 17:17:41.6626003
==================================================

//----------------------------------------------
// 2026 1학기 STL 월56 화 78
// 중간고사 4월 21일(화)
//----------------------------------------------
// STL 컨테이너 - Containers are objects that store other objects.
// Sequence Container
// - array<T, N> - 유일하게 컴파일 타임에 size 결정 - STACK, DATA
// - vector<T> - Dynamic (size) Array - free-store에 data관리
//----------------------------------------------
#include <iostream>
#include <vector>
#include <algorithm>
#include <list>
#include "save.h"
#include "ZString.h"
using namespace std;

extern bool 관찰;     //  관찰하려면 true로


//--------
int main()
//--------
{
    list<ZString> v{ "1", "22", "4444", "55555"};

    // [문제] "22"다음에 "333"을 추가하라
    관찰 = true;
    v.emplace(++++v.begin(), "333"); 
    관찰 = false;

    for (const ZString& zs : v)
        cout << zs << endl;

     save("메인.cpp");
}

==================================================
저장한 시간 - 2026-04-28 17:18:28.2976632
==================================================

//----------------------------------------------
// 2026 1학기 STL 월56 화 78
// 중간고사 4월 21일(화)
//----------------------------------------------
// STL 컨테이너 - Containers are objects that store other objects.
// Sequence Container
// - array<T, N> - 유일하게 컴파일 타임에 size 결정 - STACK, DATA
// - vector<T> - Dynamic (size) Array - free-store에 data관리
//----------------------------------------------
#include <iostream>
#include <vector>
#include <algorithm>
#include <list>
#include "save.h"
#include "ZString.h"
using namespace std;

extern bool 관찰;     //  관찰하려면 true로


//--------
int main()
//--------
{
    list<ZString> v{ "1", "22", "4444", "55555"};

    // [문제] "22"다음에 "333"을 추가하라
    관찰 = true;
    for (int i = 0; i < 10; ++i)
        v.emplace(++++v.begin(), "333");
    // 임시 객체 생성X
    관찰 = false;

    for (const ZString& zs : v)
        cout << zs << endl;

     save("메인.cpp");
}

==================================================
저장한 시간 - 2026-04-28 17:18:43.5140045
==================================================

//----------------------------------------------
// 2026 1학기 STL 월56 화 78
// 중간고사 4월 21일(화)
//----------------------------------------------
// STL 컨테이너 - Containers are objects that store other objects.
// Sequence Container
// - array<T, N> - 유일하게 컴파일 타임에 size 결정 - STACK, DATA
// - vector<T> - Dynamic (size) Array - free-store에 data관리
//----------------------------------------------
#include <iostream>
#include <vector>
#include <algorithm>
#include <list>
#include "save.h"
#include "ZString.h"
using namespace std;

extern bool 관찰;     //  관찰하려면 true로


//--------
int main()
//--------
{
    vector<ZString> v{ "1", "22", "4444", "55555"};

    // [문제] "22"다음에 "333"을 추가하라
    관찰 = true;
    for (int i = 0; i < 10; ++i)
        v.emplace(++++v.begin(), "333");
    // 임시 객체 생성X
    관찰 = false;

    for (const ZString& zs : v)
        cout << zs << endl;

     save("메인.cpp");
}

==================================================
저장한 시간 - 2026-05-04 13:41:35.9527826
==================================================

//----------------------------------------------
// 2026 1학기 STL 월56 화 78
// 중간고사 4월 21일(화)
//----------------------------------------------
// STL 컨테이너 - Containers are objects that store other objects.
// Sequence Container
// - array<T, N> - 유일하게 컴파일 타임에 size 결정 - STACK, DATA
// - vector<T> - Dynamic (size) Array - free-store에 data관리
//----------------------------------------------
#include <iostream>
#include <vector>
#include <algorithm>
#include <list>
#include "save.h"
#include "ZString.h"
using namespace std;

extern bool 관찰;     //  관찰하려면 true로


//--------
int main()
//--------
{
    list<ZString> v{ "1", "22", "4444", "55555"};

    // [문제] "22"다음에 "333"을 추가하라
    관찰 = true;
    for (int i = 0; i < 10; ++i)
        v.emplace(++++v.begin(), "333");    // 여기 빨간줄부터 시작
    // 임시 객체 생성X
    관찰 = false;

    for (const ZString& zs : v)
        cout << zs << endl;

     save("메인.cpp");
}

==================================================
저장한 시간 - 2026-05-04 13:53:06.4001428
==================================================

//----------------------------------------------
// 2026 1학기 STL 월56 화 78
// 중간고사 4월 21일(화)
//----------------------------------------------
// STL 컨테이너 - Containers are objects that store other objects.
// Sequence Container
// - array<T, N> - 유일하게 컴파일 타임에 size 결정 - STACK, DATA
// - vector<T> - Dynamic (size) Array - free-store에 data관리
//----------------------------------------------
#include <iostream>
#include <vector>
#include <algorithm>
#include <list>
#include "save.h"
#include "ZString.h"
using namespace std;

extern bool 관찰;     //  관찰하려면 true로


//--------
int main()
//--------
{
    list<ZString> v{ "1", "22", "4444", "55555"};

    // [문제] "22"다음에 "333"을 추가하라
    관찰 = true;
    auto i = v.begin();
    advance(i, 2);
    v.emplace(i, "333");
    // 임시 객체 생성X
    관찰 = false;

    for (const ZString& zs : v)
        cout << zs << endl;

     save("메인.cpp");
}

==================================================
저장한 시간 - 2026-05-04 13:55:24.9874519
==================================================

//----------------------------------------------
// 2026 1학기 STL 월56 화 78
// 중간고사 4월 21일(화)
//----------------------------------------------
// STL 컨테이너 - Containers are objects that store other objects.
// Sequence Container
// - array<T, N> - 유일하게 컴파일 타임에 size 결정 - STACK, DATA
// - vector<T> - Dynamic (size) Array - free-store에 data관리
//----------------------------------------------
#include <iostream>
#include <vector>
#include <algorithm>
#include <list>
#include "save.h"
#include "ZString.h"
using namespace std;

extern bool 관찰;     //  관찰하려면 true로


//--------
int main()
//--------
{
    list<ZString> v{ "1", "22", "4444", "55555"};

    // [문제] "22"다음에 "333"을 추가하라
    관찰 = true;
    auto p = v.begin();
    advance(p, 2);
    for (int i = 0; i < 10; ++i)
        v.emplace(p, "333");
    관찰 = false;

    for (const ZString& zs : v)
        cout << zs << endl;

     save("메인.cpp");
}

==================================================
저장한 시간 - 2026-05-04 14:08:33.0124624
==================================================

//----------------------------------------------
// 2026 1학기 STL 월56 화 78
// 중간고사 4월 21일(화)
//----------------------------------------------
// STL 컨테이너 - Containers are objects that store other objects.
// Sequence Container
// - array<T, N> - 유일하게 컴파일 타임에 size 결정 - STACK, DATA
// - vector<T> - Dynamic (size) Array - free-store에 data관리
//----------------------------------------------
#include <iostream>
#include <vector>
#include <algorithm>
#include <list>
#include "save.h"
#include "ZString.h"
using namespace std;

extern bool 관찰;     //  관찰하려면 true로


//--------
int main()
//--------
{
    save("메인.cpp");

    // [문제] v에서 길이가 2인 ZString을 삭제하라.
    vector<ZString> v{ "1", "22", "333", "44", "4444", "33", "55", "55555" };

    // 조건식은 predicate을 사용하여 판단한다.
    //predicate - bool 값을 리턴하는 callable-type
    remove_if(v.begin(), v.end(), [](const ZString& zs) {
        if (zs.size() == 2) return true;
        return false;
        });

    for (int i = 0; i < v.size(); ++i)
        cout << v[i] << endl;
}

==================================================
저장한 시간 - 2026-05-04 14:09:13.5617467
==================================================

//----------------------------------------------
// 2026 1학기 STL 월56 화 78
// 중간고사 4월 21일(화)
//----------------------------------------------
// STL 컨테이너 - Containers are objects that store other objects.
// Sequence Container
// - array<T, N> - 유일하게 컴파일 타임에 size 결정 - STACK, DATA
// - vector<T> - Dynamic (size) Array - free-store에 data관리
//----------------------------------------------
#include <iostream>
#include <vector>
#include <algorithm>
#include <list>
#include "save.h"
#include "ZString.h"
using namespace std;

extern bool 관찰;     //  관찰하려면 true로


//--------
int main()
//--------
{
    save("메인.cpp");

    // [문제] v에서 길이가 2인 ZString을 삭제하라.
    vector<ZString> v{ "1", "22", "333", "44", "4444", "33", "55", "55555" };

    // 조건식은 predicate을 사용하여 판단한다.
    //predicate - bool 값을 리턴하는 callable-type
    auto newEnd = remove_if(v.begin(), v.end(), [](const ZString& zs) {
        if (zs.size() == 2) return true;
        return false;
        });
    v.erase(newEnd, v.end());

    for (int i = 0; i < v.size(); ++i)
        cout << v[i] << endl;
}

==================================================
저장한 시간 - 2026-05-04 14:11:04.1857313
==================================================

//----------------------------------------------
// 2026 1학기 STL 월56 화 78
// 중간고사 4월 21일(화)
//----------------------------------------------
// STL 컨테이너 - Containers are objects that store other objects.
// Sequence Container
// - array<T, N> - 유일하게 컴파일 타임에 size 결정 - STACK, DATA
// - vector<T> - Dynamic (size) Array - free-store에 data관리
//----------------------------------------------
#include <iostream>
#include <vector>
#include <algorithm>
#include <list>
#include "save.h"
#include "ZString.h"
using namespace std;

extern bool 관찰;     //  관찰하려면 true로


//--------
int main()
//--------
{
    save("메인.cpp");

    // [문제] v에서 길이가 2인 ZString을 삭제하라.
    vector<ZString> v{ "1", "22", "333", "44", "4444", "33", "55", "55555" };

    // 조건식은 predicate을 사용하여 판단한다.
    //predicate - bool 값을 리턴하는 callable-type
    erase_if(v, [](const ZString& zs) {
        if (zs.size() == 2) return true;
        return false;
        });

    for (int i = 0; i < v.size(); ++i)
        cout << v[i] << endl;
}

==================================================
저장한 시간 - 2026-05-04 14:57:48.4248721
==================================================

//----------------------------------------------
// 2026 1학기 STL 월56 화 78
// 중간고사 4월 21일(화)
//----------------------------------------------
// STL 컨테이너 - Containers are objects that store other objects.
// Sequence Container
// - array<T, N> - 유일하게 컴파일 타임에 size 결정 - STACK, DATA
// - vector<T> - Dynamic (size) Array - free-store에 data관리
//             - 캐시 히트율이 높아 고속 데이터 처리에 유리
// - list<T> - 아무데서나 원소 추가/삭제 O(1)
//----------------------------------------------
#include <iostream>
#include <vector>
#include <algorithm>
#include <list>
#include <fstream>
#include "save.h"
#include "ZString.h"
using namespace std;

extern bool 관찰;     //  관찰하려면 true로

//--------
int main()
//--------
{
    save("메인.cpp");

    // [문제] 파일에 있는 단어를 list<ZString>에 저장하라.
    // 모두 몇 단어인지 출력하라.
    ifstream in{ "2026년 1학기 STL 월56 화78.txt" };
    if (not in)return 4444;

    list<ZString> words;

    ZString zs;
    while (in >> zs)
        words.push_back(zs);

    관찰 = true;
    cout << "읽은 단어 수 - " << words.size() << endl;
    words.back().show();
    관찰 = false;
}

==================================================
저장한 시간 - 2026-05-04 15:00:37.7698018
==================================================

//----------------------------------------------
// 2026 1학기 STL 월56 화 78
// 중간고사 4월 21일(화)
//----------------------------------------------
// STL 컨테이너 - Containers are objects that store other objects.
// Sequence Container
// - array<T, N> - 유일하게 컴파일 타임에 size 결정 - STACK, DATA
// - vector<T> - Dynamic (size) Array - free-store에 data관리
//             - 캐시 히트율이 높아 고속 데이터 처리에 유리
// - list<T> - 아무데서나 원소 추가/삭제 O(1)
//----------------------------------------------
#include <iostream>
#include <vector>
#include <algorithm>
#include <list>
#include <fstream>
#include "save.h"
#include "ZString.h"
using namespace std;

extern bool 관찰;     //  관찰하려면 true로

//--------
int main()
//--------
{
    save("메인.cpp");

    // [문제] 파일에 있는 단어를 list<ZString>에 저장하라.
    // 모두 몇 단어인지 출력하라.
    ifstream in{ "2026년 1학기 STL 월56 화78.txt" };
    if (not in)return 4444;

    list<ZString> words{ istream_iterator<ZString>{in}, {} };

    관찰 = true;
    cout << "읽은 단어 수 - " << words.size() << endl;
    words.back().show();
    관찰 = false;
}

==================================================
저장한 시간 - 2026-05-04 15:01:01.2947957
==================================================

//----------------------------------------------
// 2026 1학기 STL 월56 화 78
// 중간고사 4월 21일(화)
//----------------------------------------------
// STL 컨테이너 - Containers are objects that store other objects.
// Sequence Container
// - array<T, N> - 유일하게 컴파일 타임에 size 결정 - STACK, DATA
// - vector<T> - Dynamic (size) Array - free-store에 data관리
//             - 캐시 히트율이 높아 고속 데이터 처리에 유리
// - list<T> - 아무데서나 원소 추가/삭제 O(1)
//----------------------------------------------
#include <iostream>
#include <vector>
#include <algorithm>
#include <list>
#include <fstream>
#include "save.h"
#include "ZString.h"
using namespace std;

extern bool 관찰;     //  관찰하려면 true로

//--------
int main()
//--------
{
    save("메인.cpp");

    // [문제] 파일에 있는 단어를 list<ZString>에 저장하라.
    // 모두 몇 단어인지 출력하라.
    ifstream in{ "2026년 1학기 STL 월56 화78.txt" };
    if (not in)return 4444;

    vector<ZString> words{ istream_iterator<ZString>{in}, {} };

    관찰 = true;
    cout << "읽은 단어 수 - " << words.size() << endl;
    words.back().show();
    관찰 = false;
}

==================================================
저장한 시간 - 2026-05-04 15:03:02.4567523
==================================================

//----------------------------------------------
// 2026 1학기 STL 월56 화 78
// 중간고사 4월 21일(화)
//----------------------------------------------
// STL 컨테이너 - Containers are objects that store other objects.
// Sequence Container
// - array<T, N> - 유일하게 컴파일 타임에 size 결정 - STACK, DATA
// - vector<T> - Dynamic (size) Array - free-store에 data관리
//             - 캐시 히트율이 높아 고속 데이터 처리에 유리
// - list<T> - 아무데서나 원소 추가/삭제 O(1)
//----------------------------------------------
#include <iostream>
#include <vector>
#include <algorithm>
#include <list>
#include <fstream>
#include "save.h"
#include "ZString.h"
using namespace std;

extern bool 관찰;     //  관찰하려면 true로

//--------
int main()
//--------
{
    save("메인.cpp");

    // [문제] 파일에 있는 단어를 list<ZString>에 저장하라.
    // 모두 몇 단어인지 출력하라.
    ifstream in{ "2026년 1학기 STL 월56 화78.txt" };
    if (not in)return 4444;

    vector<ZString> words;
    words.reserve(50000);
    words.assign( istream_iterator<ZString>{in}, {} );
    // list - 8403
    // vector - 32839

    관찰 = true;
    cout << "읽은 단어 수 - " << words.size() << endl;
    words.back().show();
    관찰 = false;
}

==================================================
저장한 시간 - 2026-05-04 15:06:02.5837927
==================================================

//----------------------------------------------
// 2026 1학기 STL 월56 화 78
// 중간고사 4월 21일(화)
//----------------------------------------------
// STL 컨테이너 - Containers are objects that store other objects.
// Sequence Container
// - array<T, N> - 유일하게 컴파일 타임에 size 결정 - STACK, DATA
// - vector<T> - Dynamic (size) Array - free-store에 data관리
//             - 캐시 히트율이 높아 고속 데이터 처리에 유리
// - list<T> - 아무데서나 원소 추가/삭제 O(1)
//----------------------------------------------
#include <iostream>
#include <vector>
#include <algorithm>
#include <list>
#include <fstream>
#include "save.h"
#include "ZString.h"
using namespace std;

extern bool 관찰;     //  관찰하려면 true로

//--------
int main()
//--------
{
    save("메인.cpp");

    // [문제] 파일에 있는 단어를 list<ZString>에 저장하라.
    // 모두 몇 단어인지 출력하라.
    ifstream in{ "2026년 1학기 STL 월56 화78.txt" };
    if (not in)return 4444;

    vector<ZString> v;
    v.reserve(50000);
    v.assign( istream_iterator<ZString>{in}, {} );
    // list - 8403
    // vector - 32839
    // reserve하면 확연히 줄어듦.

    list<ZString> words{ v.begin(), v.end() };

    관찰 = true;
    cout << "읽은 단어 수 - " << v.size() << endl;
    v.back().show();
    관찰 = false;
}

==================================================
저장한 시간 - 2026-05-11 13:39:26.1218331
==================================================

//----------------------------------------------
// 2026 1학기 STL 월56 화 78
// 중간고사 4월 21일(화)
//----------------------------------------------
// STL 컨테이너 - Containers are objects that store other objects.
// Sequence Container
// - array<T, N> - 유일하게 컴파일 타임에 size 결정 - STACK, DATA
// - vector<T> - Dynamic (size) Array - free-store에 data관리
//             - 캐시 히트율이 높아 고속 데이터 처리에 유리
// - list<T> - 아무데서나 원소 추가/삭제 O(1)
//----------------------------------------------
#include <iostream>
#include <vector>
#include <algorithm>
#include <list>
#include <fstream>
#include "save.h"
#include "ZString.h"
using namespace std;

extern bool 관찰;     //  관찰하려면 true로

//--------
int main()
//--------
{
    save("메인.cpp");

    // [문제] 파일에 있는 단어를 list<ZString>에 저장하라.
    // 단어를 사전식 오름차순으로 정렬하라.
    ifstream in{ "2026년 1학기 STL 월56 화78.txt" };
    if (not in)return 4444;

    list<ZString> words{ istream_iterator<ZString>{in}, {} };
    // sort(words.begin(), words.end());
    
    words.sort([](const ZString& a, const ZString& b) -> bool {
        return lexicographical_compare(a.data(), a.data() + a.size(), b.data(), b.data() + b.size());  
        });
    cout << endl;

    for (const ZString& zs : words)
        cout << zs << endl;

    cout << "단어의 수 - " << words.size();
    
}

==================================================
저장한 시간 - 2026-05-11 13:43:12.7625813
==================================================

//----------------------------------------------
// 2026 1학기 STL 월56 화 78
// 중간고사 4월 21일(화)
//----------------------------------------------
// STL 컨테이너 - Containers are objects that store other objects.
// Sequence Container
// - array<T, N> - 유일하게 컴파일 타임에 size 결정 - STACK, DATA
// - vector<T> - Dynamic (size) Array - free-store에 data관리
//             - 캐시 히트율이 높아 고속 데이터 처리에 유리
// - list<T> - 아무데서나 원소 추가/삭제 O(1)
//----------------------------------------------
#include <iostream>
#include <vector>
#include <algorithm>
#include <list>
#include <fstream>
#include <chrono>
#include "save.h"
#include "ZString.h"
using namespace std;

extern bool 관찰;     //  관찰하려면 true로

//--------
int main()
//--------
{
    save("메인.cpp");

    // [문제] 파일에 있는 단어를 list<ZString>에 저장하라.
    // 단어를 사전식 오름차순으로 정렬하라.
    ifstream in{ "2026년 1학기 STL 월56 화78.txt" };
    if (not in)return 4444;

    list<ZString> words{ istream_iterator<ZString>{in}, {} };
    // sort(words.begin(), words.end());
    
    auto B = chrono::high_resolution_clock::now();
    words.sort([](const ZString& a, const ZString& b) -> bool {
        return lexicographical_compare(a.data(), a.data() + a.size(), b.data(), b.data() + b.size());  
        });
    auto E = chrono::high_resolution_clock::now();
    cout << "list 걸린 시간 - " << chrono::duration_cast<chrono::milliseconds>(E - B) << endl;

    //for (const ZString& zs : words)
    //    cout << zs << endl;

    cout << "단어의 수 - " << words.size();
    
}

==================================================
저장한 시간 - 2026-05-11 13:43:29.3464037
==================================================

//----------------------------------------------
// 2026 1학기 STL 월56 화 78
// 중간고사 4월 21일(화)
//----------------------------------------------
// STL 컨테이너 - Containers are objects that store other objects.
// Sequence Container
// - array<T, N> - 유일하게 컴파일 타임에 size 결정 - STACK, DATA
// - vector<T> - Dynamic (size) Array - free-store에 data관리
//             - 캐시 히트율이 높아 고속 데이터 처리에 유리
// - list<T> - 아무데서나 원소 추가/삭제 O(1)
//----------------------------------------------
#include <iostream>
#include <vector>
#include <algorithm>
#include <list>
#include <fstream>
#include <chrono>
#include "save.h"
#include "ZString.h"
using namespace std;

extern bool 관찰;     //  관찰하려면 true로

//--------
int main()
//--------
{
    save("메인.cpp");

    // [문제] 파일에 있는 단어를 list<ZString>에 저장하라.
    // 단어를 사전식 오름차순으로 정렬하라.
    ifstream in{ "2026년 1학기 STL 월56 화78.txt" };
    if (not in)return 4444;

    list<ZString> words{ istream_iterator<ZString>{in}, {} };
    // sort(words.begin(), words.end());
    
    auto B = chrono::high_resolution_clock::now();
    words.sort([](const ZString& a, const ZString& b) -> bool {
        return lexicographical_compare(a.data(), a.data() + a.size(), b.data(), b.data() + b.size());  
        });
    auto E = chrono::high_resolution_clock::now();
    cout << "list 걸린 시간 - " << chrono::duration_cast<chrono::microseconds>(E - B) << endl;

    //for (const ZString& zs : words)
    //    cout << zs << endl;

    cout << "단어의 수 - " << words.size();
    
}

==================================================
저장한 시간 - 2026-05-11 13:45:27.5907095
==================================================

//----------------------------------------------
// 2026 1학기 STL 월56 화 78
// 중간고사 4월 21일(화)
//----------------------------------------------
// STL 컨테이너 - Containers are objects that store other objects.
// Sequence Container
// - array<T, N> - 유일하게 컴파일 타임에 size 결정 - STACK, DATA
// - vector<T> - Dynamic (size) Array - free-store에 data관리
//             - 캐시 히트율이 높아 고속 데이터 처리에 유리
// - list<T> - 아무데서나 원소 추가/삭제 O(1)
//----------------------------------------------
#include <iostream>
#include <vector>
#include <algorithm>
#include <list>
#include <fstream>
#include <chrono>
#include "save.h"
#include "ZString.h"
using namespace std;

extern bool 관찰;     //  관찰하려면 true로

//--------
int main()
//--------
{
    save("메인.cpp");

    // [문제] 파일에 있는 단어를 list<ZString>에 저장하라.
    // 단어를 사전식 오름차순으로 정렬하라.
    ifstream in{ "2026년 1학기 STL 월56 화78.txt" };
    if (not in)return 4444;

    vector<ZString> words{ istream_iterator<ZString>{in}, {} };
    // sort(words.begin(), words.end());
    
    auto B = chrono::high_resolution_clock::now();
    sort(words.begin(), words.end(), [](const ZString& a, const ZString& b) -> bool {
        return lexicographical_compare(a.data(), a.data() + a.size(), b.data(), b.data() + b.size());  
        });
    auto E = chrono::high_resolution_clock::now();
    cout << "list 걸린 시간 - " << chrono::duration_cast<chrono::microseconds>(E - B) << endl;
   

    //for (const ZString& zs : words)
    //    cout << zs << endl;

    cout << "단어의 수 - " << words.size();
    

    // list 정렬 시간 - 1090us
    // vector 정렬 시간 - 
}

==================================================
저장한 시간 - 2026-05-11 13:46:40.9998361
==================================================

//----------------------------------------------
// 2026 1학기 STL 월56 화 78
// 중간고사 4월 21일(화)
//----------------------------------------------
// STL 컨테이너 - Containers are objects that store other objects.
// Sequence Container
// - array<T, N> - 유일하게 컴파일 타임에 size 결정 - STACK, DATA
// - vector<T> - Dynamic (size) Array - free-store에 data관리
//             - 캐시 히트율이 높아 고속 데이터 처리에 유리
// - list<T> - 아무데서나 원소 추가/삭제 O(1)
//----------------------------------------------
#include <iostream>
#include <vector>
#include <algorithm>
#include <list>
#include <fstream>
#include <chrono>
#include "save.h"
#include "ZString.h"
using namespace std;

extern bool 관찰;     //  관찰하려면 true로

//--------
int main()
//--------
{
    save("메인.cpp");

    // [문제] 파일에 있는 단어를 list<ZString>에 저장하라.
    // 단어를 사전식 오름차순으로 정렬하라.
    ifstream in{ "2026년 1학기 STL 월56 화78.txt" };
    if (not in)return 4444;

    vector<ZString> words{ istream_iterator<ZString>{in}, {} };
    // sort(words.begin(), words.end());
    
    auto B = chrono::high_resolution_clock::now();
    //words.sort([](const ZString& a, const ZString& b) -> bool {
    //    return lexicographical_compare(a.data(), a.data() + a.size(), b.data(), b.data() + b.size());
    //    });
    sort(words.begin(), words.end(), [](const ZString& a, const ZString& b) -> bool {
        return lexicographical_compare(a.data(), a.data() + a.size(), b.data(), b.data() + b.size());  
        });
    auto E = chrono::high_resolution_clock::now();
    cout << "list 걸린 시간 - " << chrono::duration_cast<chrono::microseconds>(E - B) << endl;
   

    //for (const ZString& zs : words)
    //    cout << zs << endl;

    cout << "단어의 수 - " << words.size();
    

    // list 정렬 시간 - 1090us
    // vector 정렬 시간 - 849us
}

==================================================
저장한 시간 - 2026-05-11 13:58:08.0131019
==================================================

//----------------------------------------------
// 2026 1학기 STL 월56 화 78
// 중간고사 4월 21일(화)
//----------------------------------------------
// STL 컨테이너 - Containers are objects that store other objects.
// Sequence Container
// - array<T, N> - 유일하게 컴파일 타임에 size 결정 - STACK, DATA
// - vector<T> - Dynamic (size) Array - free-store에 data관리
//             - 캐시 히트율이 높아 고속 데이터 처리에 유리
// - list<T> - 아무데서나 원소 추가/삭제 O(1)
//----------------------------------------------
#include <iostream>
#include <vector>
#include <algorithm>
#include <list>
#include <fstream>
#include <chrono>
#include "save.h"
#include "ZString.h"
using namespace std;

extern bool 관찰;     //  관찰하려면 true로

//--------
int main()
//--------
{
    save("메인.cpp");

    // [문제] 파일에 있는 단어를 list<ZString>에 저장하라.
    // 단어를 사전식 오름차순으로 정렬하라.
    ifstream in{ "2026년 1학기 STL 월56 화78.txt" };
    if (not in)return 4444;

    list<ZString> words{ istream_iterator<ZString>{in}, {} };
    
    words.sort([](const ZString& a, const ZString& b) -> bool {
        return lexicographical_compare(a.data(), a.data() + a.size(), b.data(), b.data() + b.size());
        });
   
    // 정렬한 이후라면 중복 단어 제거
    // default operator== 로 판정
    words.unique();

    for (const ZString& zs : words)
        cout << zs << endl;

    cout << "중복을 제거하고 남은 단어의 수 - " << words.size();
    

    // list 정렬 시간 - 1090us
    // list는 merge기반 sort
    
    // vector 정렬 시간 - 849us
    // vector는 quick sort
    // 이동 연산자가 없는 vector - 5105us
}

==================================================
저장한 시간 - 2026-05-11 13:59:00.7888911
==================================================

//----------------------------------------------
// 2026 1학기 STL 월56 화 78
// 중간고사 4월 21일(화)
//----------------------------------------------
// STL 컨테이너 - Containers are objects that store other objects.
// Sequence Container
// - array<T, N> - 유일하게 컴파일 타임에 size 결정 - STACK, DATA
// - vector<T> - Dynamic (size) Array - free-store에 data관리
//             - 캐시 히트율이 높아 고속 데이터 처리에 유리
// - list<T> - 아무데서나 원소 추가/삭제 O(1)
//----------------------------------------------
#include <iostream>
#include <vector>
#include <algorithm>
#include <list>
#include <fstream>
#include <chrono>
#include "save.h"
#include "ZString.h"
using namespace std;

extern bool 관찰;     //  관찰하려면 true로

//--------
int main()
//--------
{
    save("메인.cpp");

    // [문제] 파일에 있는 단어를 list<ZString>에 저장하라.
    // 단어를 사전식 오름차순으로 정렬하라.
    ifstream in{ "2026년 1학기 STL 월56 화78.txt" };
    if (not in)return 4444;

    list<ZString> words{ istream_iterator<ZString>{in}, {} };
    
    words.sort([](const ZString& a, const ZString& b) -> bool {
        return lexicographical_compare(a.data(), a.data() + a.size(), b.data(), b.data() + b.size());
        });
   
    // 정렬한 이후라면 중복 단어 제거
    // default operator== 로 판정
    words.unique();

    for (const ZString& zs : words)
        cout << zs << endl;

    cout << "중복을 제거하고 남은 단어의 수 - " << words.size();
    

    // list 정렬 시간 - 1090us
    // list는 merge기반 sort
    
    // vector 정렬 시간 - 849us
    // vector는 quick sort
    // 이동 연산자가 없는 vector - 5105us
}

==================================================
저장한 시간 - 2026-05-11 14:05:40.1347061
==================================================

//----------------------------------------------
// 2026 1학기 STL 월56 화 78
// 중간고사 4월 21일(화)
//----------------------------------------------
// STL 컨테이너 - Containers are objects that store other objects.
// Sequence Container
// - array<T, N> - 유일하게 컴파일 타임에 size 결정 - STACK, DATA
// - vector<T> - Dynamic (size) Array - free-store에 data관리
//             - 캐시 히트율이 높아 고속 데이터 처리에 유리
// - list<T> - 아무데서나 원소 추가/삭제 O(1)
//----------------------------------------------
#include <iostream>
#include <vector>
#include <algorithm>
#include <list>
#include <fstream>
#include <chrono>
#include "save.h"
#include "ZString.h"
using namespace std;

extern bool 관찰;     //  관찰하려면 true로

//--------
int main()
//--------
{
    save("메인.cpp");

    // [문제] 파일에 있는 단어를 list<ZString>에 저장하라.
    // 단어를 사전식 오름차순으로 정렬하라.
    ifstream in{ "2026년 1학기 STL 월56 화78.txt" };
    if (not in)return 4444;

    list<ZString> words{ istream_iterator<ZString>{in}, {} };
    
    words.sort([](const ZString& a, const ZString& b) -> bool {
        return lexicographical_compare(a.data(), a.data() + a.size(), b.data(), b.data() + b.size());
        });
   
    // 정렬한 이후라면 중복 단어 제거
    // default operator== 로 판정
    words.unique();

    for (const ZString& zs : words)
        cout << zs << endl;

    cout << "중복을 제거하고 남은 단어의 수 - " << words.size();
    
    // [문제] 사용자가 입력한 단어가 리스트에 있는지 찾아본다.
    // 없으면 없다고 출력하라.
    // 있다면 리스트의 몇번째 단어인지 출력하라.

    cout << "찾을 단어? : ";
    ZString word;
    cin >> word;

    find(words.begin(), words.end(), word);
}

==================================================
저장한 시간 - 2026-05-11 14:10:32.4213007
==================================================

//----------------------------------------------
// 2026 1학기 STL 월56 화 78
// 중간고사 4월 21일(화)
//----------------------------------------------
// STL 컨테이너 - Containers are objects that store other objects.
// Sequence Container
// - array<T, N> - 유일하게 컴파일 타임에 size 결정 - STACK, DATA
// - vector<T> - Dynamic (size) Array - free-store에 data관리
//             - 캐시 히트율이 높아 고속 데이터 처리에 유리
// - list<T> - 아무데서나 원소 추가/삭제 O(1)
//----------------------------------------------
#include <iostream>
#include <vector>
#include <algorithm>
#include <list>
#include <fstream>
#include <chrono>
#include <ranges>
#include "save.h"
#include "ZString.h"
using namespace std;

extern bool 관찰;     //  관찰하려면 true로

//--------
int main()
//--------
{
    save("메인.cpp");

    // [문제] 파일에 있는 단어를 list<ZString>에 저장하라.
    // 단어를 사전식 오름차순으로 정렬하라.
    ifstream in{ "2026년 1학기 STL 월56 화78.txt" };
    if (not in)return 4444;

    list<ZString> words{ istream_iterator<ZString>{in}, {} };
    
    words.sort([](const ZString& a, const ZString& b) -> bool {
        return lexicographical_compare(a.data(), a.data() + a.size(), b.data(), b.data() + b.size());
        });
   
    // 정렬한 이후라면 중복 단어 제거
    // default operator== 로 판정
    words.unique();

    for (const ZString& zs : words
        | views::take(5)
        | views::drop(words.size() - 10)
        | views::take(5) )
        cout << zs << endl;

    cout << "중복을 제거하고 남은 단어의 수 - " << words.size() << endl;
    
    // [문제] 사용자가 입력한 단어가 리스트에 있는지 찾아본다.
    // 없으면 없다고 출력하라.
    // 있다면 리스트의 몇번째 단어인지 출력하라.

    cout << "찾을 단어? : ";
    ZString word;
    cin >> word;

    list<ZString>::iterator pos = find(words.begin(), words.end(), word);

    if (pos == words.end())
        cout << "word는 없는 단어입니다." << endl;
    else {
        cout << distance(pos, words.begin()) + 1 << "번쨰 단어입니다." << endl;
    }
}

==================================================
저장한 시간 - 2026-05-11 14:11:56.8498035
==================================================

//----------------------------------------------
// 2026 1학기 STL 월56 화 78
// 중간고사 4월 21일(화)
//----------------------------------------------
// STL 컨테이너 - Containers are objects that store other objects.
// Sequence Container
// - array<T, N> - 유일하게 컴파일 타임에 size 결정 - STACK, DATA
// - vector<T> - Dynamic (size) Array - free-store에 data관리
//             - 캐시 히트율이 높아 고속 데이터 처리에 유리
// - list<T> - 아무데서나 원소 추가/삭제 O(1)
//----------------------------------------------
#include <iostream>
#include <vector>
#include <algorithm>
#include <list>
#include <fstream>
#include <chrono>
#include <ranges>
#include "save.h"
#include "ZString.h"
using namespace std;

extern bool 관찰;     //  관찰하려면 true로

//--------
int main()
//--------
{
    save("메인.cpp");

    // [문제] 파일에 있는 단어를 list<ZString>에 저장하라.
    // 단어를 사전식 오름차순으로 정렬하라.
    ifstream in{ "2026년 1학기 STL 월56 화78.txt" };
    if (not in)return 4444;

    list<ZString> words{ istream_iterator<ZString>{in}, {} };
    
    words.sort([](const ZString& a, const ZString& b) -> bool {
        return lexicographical_compare(a.data(), a.data() + a.size(), b.data(), b.data() + b.size());
        });
   
    // 정렬한 이후라면 중복 단어 제거
    // default operator== 로 판정
    words.unique();

    for (const ZString& zs : words
        | views::take(5) )
        cout << zs << endl;

    for (const ZString& zs : words
        | views::reverse
        | views::take(5))
        cout << zs << endl;

    cout << "중복을 제거하고 남은 단어의 수 - " << words.size() << endl;
    
    // [문제] 사용자가 입력한 단어가 리스트에 있는지 찾아본다.
    // 없으면 없다고 출력하라.
    // 있다면 리스트의 몇번째 단어인지 출력하라.

    cout << "찾을 단어? : ";
    ZString word;
    cin >> word;

    list<ZString>::iterator pos = find(words.begin(), words.end(), word);

    if (pos == words.end())
        cout << "word는 없는 단어입니다." << endl;
    else {
        cout << distance(pos, words.begin()) + 1 << "번쨰 단어입니다." << endl;
    }
}

==================================================
저장한 시간 - 2026-05-11 14:12:14.0087302
==================================================

//----------------------------------------------
// 2026 1학기 STL 월56 화 78
// 중간고사 4월 21일(화)
//----------------------------------------------
// STL 컨테이너 - Containers are objects that store other objects.
// Sequence Container
// - array<T, N> - 유일하게 컴파일 타임에 size 결정 - STACK, DATA
// - vector<T> - Dynamic (size) Array - free-store에 data관리
//             - 캐시 히트율이 높아 고속 데이터 처리에 유리
// - list<T> - 아무데서나 원소 추가/삭제 O(1)
//----------------------------------------------
#include <iostream>
#include <vector>
#include <algorithm>
#include <list>
#include <fstream>
#include <chrono>
#include <ranges>
#include "save.h"
#include "ZString.h"
using namespace std;

extern bool 관찰;     //  관찰하려면 true로

//--------
int main()
//--------
{
    save("메인.cpp");

    // [문제] 파일에 있는 단어를 list<ZString>에 저장하라.
    // 단어를 사전식 오름차순으로 정렬하라.
    ifstream in{ "2026년 1학기 STL 월56 화78.txt" };
    if (not in)return 4444;

    list<ZString> words{ istream_iterator<ZString>{in}, {} };
    
    words.sort([](const ZString& a, const ZString& b) -> bool {
        return lexicographical_compare(a.data(), a.data() + a.size(), b.data(), b.data() + b.size());
        });
   
    // 정렬한 이후라면 중복 단어 제거
    // default operator== 로 판정
    words.unique();

    for (const ZString& zs : words
        | views::take(5) )
        cout << zs << endl;

    cout << "생략 ==================================" << endl;

    for (const ZString& zs : words
        | views::reverse
        | views::take(5))
        cout << zs << endl;

    cout << "중복을 제거하고 남은 단어의 수 - " << words.size() << endl;
    
    // [문제] 사용자가 입력한 단어가 리스트에 있는지 찾아본다.
    // 없으면 없다고 출력하라.
    // 있다면 리스트의 몇번째 단어인지 출력하라.

    cout << "찾을 단어? : ";
    ZString word;
    cin >> word;

    list<ZString>::iterator pos = find(words.begin(), words.end(), word);

    if (pos == words.end())
        cout << "word는 없는 단어입니다." << endl;
    else {
        cout << distance(pos, words.begin()) + 1 << "번쨰 단어입니다." << endl;
    }
}

==================================================
저장한 시간 - 2026-05-11 14:12:30.4750614
==================================================

//----------------------------------------------
// 2026 1학기 STL 월56 화 78
// 중간고사 4월 21일(화)
//----------------------------------------------
// STL 컨테이너 - Containers are objects that store other objects.
// Sequence Container
// - array<T, N> - 유일하게 컴파일 타임에 size 결정 - STACK, DATA
// - vector<T> - Dynamic (size) Array - free-store에 data관리
//             - 캐시 히트율이 높아 고속 데이터 처리에 유리
// - list<T> - 아무데서나 원소 추가/삭제 O(1)
//----------------------------------------------
#include <iostream>
#include <vector>
#include <algorithm>
#include <list>
#include <fstream>
#include <chrono>
#include <ranges>
#include "save.h"
#include "ZString.h"
using namespace std;

extern bool 관찰;     //  관찰하려면 true로

//--------
int main()
//--------
{
    save("메인.cpp");

    // [문제] 파일에 있는 단어를 list<ZString>에 저장하라.
    // 단어를 사전식 오름차순으로 정렬하라.
    ifstream in{ "2026년 1학기 STL 월56 화78.txt" };
    if (not in)return 4444;

    list<ZString> words{ istream_iterator<ZString>{in}, {} };
    
    words.sort([](const ZString& a, const ZString& b) -> bool {
        return lexicographical_compare(a.data(), a.data() + a.size(), b.data(), b.data() + b.size());
        });
   
    // 정렬한 이후라면 중복 단어 제거
    // default operator== 로 판정
    words.unique();

    for (const ZString& zs : words
        | views::take(5) )
        cout << zs << endl;

    cout << "생략 ==================================" << endl;

    for (const ZString& zs : words
        | views::reverse
        | views::take(5))
        cout << zs << endl;

    cout << "중복을 제거하고 남은 단어의 수 - " << words.size() << endl;
    
    // [문제] 사용자가 입력한 단어가 리스트에 있는지 찾아본다.
    // 없으면 없다고 출력하라.
    // 있다면 리스트의 몇번째 단어인지 출력하라.

    cout << "찾을 단어? : ";
    ZString word;
    cin >> word;

    list<ZString>::iterator pos = find(words.begin(), words.end(), word);

    if (pos == words.end())
        cout << "word는 없는 단어입니다." << endl;
    else {
        cout << distance(pos, words.begin()) + 1 << "번쨰 단어입니다." << endl;
    }
}

==================================================
저장한 시간 - 2026-05-11 14:14:34.8936465
==================================================

//----------------------------------------------
// 2026 1학기 STL 월56 화 78
// 중간고사 4월 21일(화)
//----------------------------------------------
// STL 컨테이너 - Containers are objects that store other objects.
// Sequence Container
// - array<T, N> - 유일하게 컴파일 타임에 size 결정 - STACK, DATA
// - vector<T> - Dynamic (size) Array - free-store에 data관리
//             - 캐시 히트율이 높아 고속 데이터 처리에 유리
// - list<T> - 아무데서나 원소 추가/삭제 O(1)
//----------------------------------------------
#include <iostream>
#include <vector>
#include <algorithm>
#include <list>
#include <fstream>
#include <chrono>
#include <ranges>
#include "save.h"
#include "ZString.h"
using namespace std;

extern bool 관찰;     //  관찰하려면 true로

//--------
int main()
//--------
{
    save("메인.cpp");

    // [문제] 파일에 있는 단어를 list<ZString>에 저장하라.
    // 단어를 사전식 오름차순으로 정렬하라.
    ifstream in{ "2026년 1학기 STL 월56 화78.txt" };
    if (not in)return 4444;

    list<ZString> words{ istream_iterator<ZString>{in}, {} };
    
    words.sort([](const ZString& a, const ZString& b) -> bool {
        return lexicographical_compare(a.data(), a.data() + a.size(), b.data(), b.data() + b.size());
        });
   
    // 정렬한 이후라면 중복 단어 제거
    // default operator== 로 판정
    words.unique();

    for (const ZString& zs : words
        | views::take(5) )
        cout << zs << endl;

    cout << "-----------------------------------------" << endl;

    for (const ZString& zs : words
        | views::reverse
        | views::take(5))
        cout << zs << endl;

    cout << "중복을 제거하고 남은 단어의 수 - " << words.size() << endl;
    
    // [문제] 사용자가 입력한 단어가 리스트에 있는지 찾아본다.
    // 없으면 없다고 출력하라.
    // 있다면 리스트의 몇번째 단어인지 출력하라.

    cout << "찾을 단어? : ";
    ZString word;
    cin >> word;

    list<ZString>::iterator pos = find(words.begin(), words.end(), word);

    if (pos == words.end())
        cout << "word는 없는 단어입니다." << endl;
    else {
        cout << distance(words.begin(), pos) + 1 << "번쨰 단어입니다." << endl;
    }
}

==================================================
저장한 시간 - 2026-05-11 14:16:22.6487645
==================================================

//----------------------------------------------
// 2026 1학기 STL 월56 화 78
// 중간고사 4월 21일(화)
//----------------------------------------------
// STL 컨테이너 - Containers are objects that store other objects.
// Sequence Container
// - array<T, N> - 유일하게 컴파일 타임에 size 결정 - STACK, DATA
// - vector<T> - Dynamic (size) Array - free-store에 data관리
//             - 캐시 히트율이 높아 고속 데이터 처리에 유리
// - list<T> - 아무데서나 원소 추가/삭제 O(1)
//----------------------------------------------
#include <iostream>
#include <vector>
#include <algorithm>
#include <list>
#include <fstream>
#include <chrono>
#include <ranges>
#include "save.h"
#include "ZString.h"
using namespace std;

extern bool 관찰;     //  관찰하려면 true로

//--------
int main()
//--------
{
    save("메인.cpp");

    // [문제] 파일에 있는 단어를 list<ZString>에 저장하라.
    // 단어를 사전식 오름차순으로 정렬하라.
    ifstream in{ "2026년 1학기 STL 월56 화78.txt" };
    if (not in)return 4444;

    vector<ZString> words{ istream_iterator<ZString>{in}, {} };
    
    sort(words.begin(), words.end(), [](const ZString& a, const ZString& b) -> bool {
        return lexicographical_compare(a.data(), a.data() + a.size(), b.data(), b.data() + b.size());
        });
   
    // 정렬한 이후라면 중복 단어 제거
    // default operator== 로 판정
    auto newEnd = unique(words.begin(), words.end());
    words.erase(newEnd, words.end());

    for (const ZString& zs : words
        | views::take(5) )
        cout << zs << endl;

    cout << "-----------------------------------------" << endl;

    for (const ZString& zs : words
        | views::reverse
        | views::take(5))
        cout << zs << endl;

    cout << "중복을 제거하고 남은 단어의 수 - " << words.size() << endl;
    
    // [문제] 사용자가 입력한 단어가 리스트에 있는지 찾아본다.
    // 없으면 없다고 출력하라.
    // 있다면 리스트의 몇번째 단어인지 출력하라.

    cout << "찾을 단어? : ";
    ZString word;
    cin >> word;

    auto pos = find(words.begin(), words.end(), word);

    if (pos == words.end())
        cout << "word는 없는 단어입니다." << endl;
    else {
        cout << distance(words.begin(), pos) + 1 << "번쨰 단어입니다." << endl;
    }
}

==================================================
저장한 시간 - 2026-05-11 14:16:32.5851227
==================================================

//----------------------------------------------
// 2026 1학기 STL 월56 화 78
// 중간고사 4월 21일(화)
//----------------------------------------------
// STL 컨테이너 - Containers are objects that store other objects.
// Sequence Container
// - array<T, N> - 유일하게 컴파일 타임에 size 결정 - STACK, DATA
// - vector<T> - Dynamic (size) Array - free-store에 data관리
//             - 캐시 히트율이 높아 고속 데이터 처리에 유리
// - list<T> - 아무데서나 원소 추가/삭제 O(1)
//----------------------------------------------
#include <iostream>
#include <vector>
#include <algorithm>
#include <list>
#include <fstream>
#include <chrono>
#include <ranges>
#include "save.h"
#include "ZString.h"
using namespace std;

extern bool 관찰;     //  관찰하려면 true로

//--------
int main()
//--------
{
    save("메인.cpp");

    // [문제] 파일에 있는 단어를 list<ZString>에 저장하라.
    // 단어를 사전식 오름차순으로 정렬하라.
    ifstream in{ "2026년 1학기 STL 월56 화78.txt" };
    if (not in)return 4444;

    vector<ZString> words{ istream_iterator<ZString>{in}, {} };
    
    sort(words.begin(), words.end(), [](const ZString& a, const ZString& b) -> bool {
        return lexicographical_compare(a.data(), a.data() + a.size(), b.data(), b.data() + b.size());
        });
   
    // 정렬한 이후라면 중복 단어 제거
    // default operator== 로 판정
    auto newEnd = unique(words.begin(), words.end());
    words.erase(newEnd, words.end());

    for (const ZString& zs : words
        | views::take(5) )
        cout << zs << endl;

    cout << "-----------------------------------------" << endl;

    for (const ZString& zs : words
        | views::reverse
        | views::take(5))
        cout << zs << endl;

    cout << "중복을 제거하고 남은 단어의 수 - " << words.size() << endl;
    
    // [문제] 사용자가 입력한 단어가 리스트에 있는지 찾아본다.
    // 없으면 없다고 출력하라.
    // 있다면 리스트의 몇번째 단어인지 출력하라.

    cout << "찾을 단어? : ";
    ZString word;
    cin >> word;

    auto pos = find(words.begin(), words.end(), word);

    if (pos == words.end())
        cout << "word는 없는 단어입니다." << endl;
    else {
        cout << distance(words.begin(), pos) + 1 << "번쨰 단어입니다." << endl;
    }
}

==================================================
저장한 시간 - 2026-05-11 14:16:48.7059736
==================================================

//----------------------------------------------
// 2026 1학기 STL 월56 화 78
// 중간고사 4월 21일(화)
//----------------------------------------------
// STL 컨테이너 - Containers are objects that store other objects.
// Sequence Container
// - array<T, N> - 유일하게 컴파일 타임에 size 결정 - STACK, DATA
// - vector<T> - Dynamic (size) Array - free-store에 data관리
//             - 캐시 히트율이 높아 고속 데이터 처리에 유리
// - list<T> - 아무데서나 원소 추가/삭제 O(1)
//----------------------------------------------
#include <iostream>
#include <vector>
#include <algorithm>
#include <list>
#include <fstream>
#include <chrono>
#include <ranges>
#include "save.h"
#include "ZString.h"
using namespace std;

extern bool 관찰;     //  관찰하려면 true로

//--------
int main()
//--------
{
    save("메인.cpp");

    // [문제] 파일에 있는 단어를 list<ZString>에 저장하라.
    // 단어를 사전식 오름차순으로 정렬하라.
    ifstream in{ "2026년 1학기 STL 월56 화78.txt" };
    if (not in)return 4444;

    vector<ZString> words{ istream_iterator<ZString>{in}, {} };
    
    sort(words.begin(), words.end(), [](const ZString& a, const ZString& b) -> bool {
        return lexicographical_compare(a.data(), a.data() + a.size(), b.data(), b.data() + b.size());
        });
   
    // 정렬한 이후라면 중복 단어 제거
    // default operator== 로 판정
    auto newEnd = unique(words.begin(), words.end());
    words.erase(newEnd, words.end());

    for (const ZString& zs : words
        | views::take(5) )
        cout << zs << endl;

    cout << "-----------------------------------------" << endl;

    for (const ZString& zs : words
        | views::reverse
        | views::take(5))
        cout << zs << endl;

    cout << "중복을 제거하고 남은 단어의 수 - " << words.size() << endl;
    
    // [문제] 사용자가 입력한 단어가 리스트에 있는지 찾아본다.
    // 없으면 없다고 출력하라.
    // 있다면 리스트의 몇번째 단어인지 출력하라.

    while (true) {
        cout << "찾을 단어? : ";
        ZString word;
        cin >> word;

        auto pos = find(words.begin(), words.end(), word);

        if (pos == words.end())
            cout << "word는 없는 단어입니다." << endl;
        else {
            cout << distance(words.begin(), pos) + 1 << "번쨰 단어입니다." << endl;
        }
    }
}